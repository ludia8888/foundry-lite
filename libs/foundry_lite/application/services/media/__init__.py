"""Media/Content Plane application services (ADR-0001).

Local MediaSet transaction core: catalog lifecycle, immutable staged uploads, the
atomic metadata-pointer commit, and version-pinned reference resolution.
"""
