# Supply Chain Demo

This demo proves the Foundry-lite closed loop with Order, Customer, ApproveOrder, ops.action_log, and ops.order_current.

Run it with `pnpm demo:supply-chain`. The command uses an isolated fresh demo home by default, so repeated runs prove the closed loop instead of depending on old local state.
