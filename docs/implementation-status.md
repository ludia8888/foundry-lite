# Implementation Status

**Status:** Current implementation boundary / source of truth for current, partial, and future claims
**Audience:** Maintainers, reviewers, frontend planners, and operators checking what the current checkout truly proves

이 문서는 현재 커밋이 실제로 보장하는 것과, 계획 문서에 남아 있는 다음 목표를 구분한다. 스프린트 체크박스별 PR, merge commit, 테스트, 품질 게이트 근거는 [Sprint Evidence Ledger](./sprint-evidence-ledger.md)를 따른다.

## MVP Core Boundary

이 섹션은 이전 `docs/implementation-status.md`의 v1 core 범위를 이 문서로 병합한 것이다. 이제 MVP 포함/제외
판단은 별도 scope 문서가 아니라 이 implementation status 문서와 sprint evidence ledger를 함께 본다.

Included in the Sprint 00-36 v1 core:

- CSV upload for raw datasets.
- SQLite plus local filesystem storage for the default local adapter.
- Immutable dataset transaction and manifest commit protocol.
- DuckDB SQL transforms with input version binding and lineage.
- Ontology YAML import, validation, and activation.
- Order and Customer object indexing.
- Object query, link traversal, and saved-set-ready query model.
- ApproveOrder action with required parameters, safe precondition evaluation, idempotency, optimistic concurrency, audit, object edits, and outbox events.
- `ops.action_log` and `ops.order_current` materialization.
- Downstream `customer_risk` transform that consumes materialized operational state.
- Scale Foundation boundaries for storage, metadata, compute, event, search, workflow, connector, and auth, so later infra can be swapped behind ports/adapters.

Implemented outside the v1 core gate, with current evidence but separate production packaging/worker/managed-infra work:

- REST pull connector adapter and signed webhook append ingest.
- Local/fake stream archive writer, Kafka-compatible adapter, and one-shot worker proof.
- Debezium-shaped CDC archive proof, live Debezium/PostgreSQL topic proof, CDC object indexing, and active S3+Iceberg+Spark+CDC composition proof.
- Elasticsearch-compatible search projection, rebuild, orphan-drift detection, object-store fallback proof, and live Testcontainers proof.
- Shadow reindex, active index pointer, count/hash validation, and action-edit replay proof.
- S3/MinIO, Iceberg, Spark, Temporal, and Elasticsearch infra ratchet proofs.
- Ontology-driven sensitive classification and masking across object/query/search/preview/explain/action audit surfaces.
- Runtime profile packaging extras for S3, Iceberg, Spark, Temporal, Elasticsearch, Kafka, and PostgreSQL.
- Alembic baseline migration parity for a fresh DB, guarded by migration/schema gates.

Explicitly deferred beyond the v1 core:

- PostgreSQL JSONB production object store and PostgreSQL snapshot connector implementation.
- Multi-step Alembic upgrade/rollback workflows and production migration runbooks beyond the current parity/safety gates.
- Real CEL or JSON Logic evaluator.
- Real external ERP/webhook writeback and vendor remote lookup/compensation worker execution.
- Continuously running Kafka/Redpanda stream workers, CDC object-indexing workers, and deployment-specific broker/CDC packaging.
- Managed Elasticsearch deployment and operations beyond the current adapter/projection/live-cluster proof.
- Iceberg maintenance execution, production catalog operations, and managed retention/compaction beyond the current planning/storage ratchet.
- Real Spark cluster deployment and distributed failure modes beyond the current local adapter/composition ratchet.
- Full connector activity data-plane execution through Temporal and managed worker operations.
- Kubernetes Helm, backup, restore, operational runbooks, production Flink runner, full visual pipeline builder, complex CBAC/ABAC security, and Functions-on-Objects runtime.

## Current Reality

- Storage adapter: local SQLite database plus filesystem object storage by default; optional MinIO/S3-compatible object storage is available through `S3DatasetStorageAdapter` and the `s3-storage` adapter profile. The second infra ratchet adds `IcebergDatasetStorageAdapter` and the `iceberg` profile: each dataset version is one Iceberg table snapshot (pinned by `snapshot_id`) with data/metadata in object storage and a SQL catalog tracking the pointer, covered by `quality:iceberg` against MinIO/Testcontainers. Iceberg now keeps Foundry sidecar manifests beside the table data so commit-time branch/created_at and real object-byte hashes are recoverable on read; duplicate-version guard catalog timeouts fail closed as retryable adapter failures. The third infra ratchet adds `SparkComputeAdapter` and the `spark` compute profile (selected independently of storage via `FOUNDRY_LITE_COMPUTE_PROFILE`): Spark runs CSV ingest and SQL transforms on local parquet that the engine materializes from any storage profile, output commits through the dataset transaction protocol, same-session concurrent transforms use per-run temp views, and Spark row-batch CSV staging preserves quoted JSON/CDC strings, covered by `quality:spark`. The composition ratchet is now explicit: `quality:infra-composition` boots `adapter_profile=iceberg` with `FOUNDRY_LITE_COMPUTE_PROFILE=spark` against a MinIO warehouse so Spark reads Iceberg-on-S3 pinned inputs and commits output back to Iceberg-on-S3, including a failure→abort proof. The same composition gate now covers Debezium CDC on the active stack: Debezium-normalized events archive into an Iceberg-on-S3 changelog through Spark, replay into CDC object indexing, skip a stale post-delete update, materialize `ops.order_current`, and prove archive writer failure leaves no raw CDC dataset version. Genuinely distributed Spark failure modes need a real cluster and are deferred. CI now acts as an automated reviewer: proof-matrix, source-of-truth, and operator-evidence contract gates read the single `docs/infra-tricky-matrix.json` and emit root-cause-style failures with suggested files; `quality:documentation-map` keeps the documentation operating map, README document index, core source-of-truth rows, and cross-check commands from drifting; these gates run in the `ci:gate:runtime` or `ci:gate:static` lanes for PR feedback and in `ci:gate:release` for push/tags/dispatch evidence (`release.yml`, heavier perf smokes + uploaded artifacts).
- Object properties: SQLAlchemy `JSON` columns, not PostgreSQL `JSONB`.
- Commit-point risk tracking: [Commit-Point Risk Register](./commit-point-risk-register.md) now separates "feature works" from "the durable commit-point invariant is proven." Materialization watermarks have been hardened for the current MVP path: `action_log` pins `completed_at` plus `action_run_id`, and `object_snapshot` pins `active_index_version` plus tenant-scoped `object_change_sequence` while capturing output rows at run start. Object changes now append `object_record_versions`, so an old `object_snapshot` run can replay its stored watermark after later in-place object updates and still produce the original row hash/state. This prevents late-committed actions from being skipped, keeps failed action runs out of successful `ops.action_log` materialization rows, keeps same-cursor `action_log` reruns from duplicating rows inside the output, prevents a mid-run action from partly entering the same `ops.order_current` output, and makes old snapshot watermarks durable enough for replay evidence. Materialization metadata/outbox/lineage failure after storage promotion removes the promoted artifact, exposes no `materialization.completed` event for the failed run, and lets a same-cursor retry commit exactly one output version. Dataset/storage split-brain protection has also been hardened for the local MVP and MinIO/S3-compatible paths: storage promotion followed by DB metadata failure removes the promoted version artifact, FAILED sync runs keep `orphan_cleanup` evidence, committed versions with missing manifest/data files raise `committed_version_storage_missing`, local and S3 adapters verify manifest URI, byte size, and content hash after write, S3 partial multipart timeout cleanup prevents a partial object from becoming a committed version, same-version retry creates exactly one manifest/data pair, and S3 storage failures surface `adapterFailure` evidence in Operations. Transform output/lineage atomicity now uses the dataset finalization boundary for post-commit metadata: if lineage insertion fails after output storage promotion, the promoted artifact is removed, no output version or lineage edge is served, and the transform run is marked `FAILED` with cleanup evidence. Compute-stage transform failure also deletes transaction staging artifacts, so a DuckDB OOM-like failure after writing a partial staged output leaves no committed output version and no partial staged file. SQL transform execution rejects raw filesystem read functions such as DuckDB CSV path reads; transforms must use declared dataset inputs through the `{{ input('namespace.name') }}` template boundary. Python transforms are fail-closed at registration until a sandboxed SDK input/output abstraction exists, so user Python code cannot receive raw storage paths or credentials today. Stream/REST cursor commit-point proof now covers the local failure-injection path: if a stream batch or REST page is read but dataset writing fails, retry resumes from the last committed offset/cursor rather than the failed attempt's next cursor. Action idempotency request-fingerprint proof now covers the current action path: same-key replay requires the same canonical request fingerprint, while same-key/different-body reuse returns `ConflictDetected` and leaves `action.run.idempotency_conflict` audit evidence. Action commit atomicity proof now covers the current internal DB action path: failures at object mutation/edit/status/outbox/audit boundaries roll back together and no partial action evidence survives; the action idempotency winner insert now uses conflict-ignore semantics instead of a savepoint that could escape outer rollback on SQLite. Remaining gaps: S3 Parquet footer-specific validation can be added beyond byte/hash read-back, stream live-broker rebalance/commit-unknown edges need failure injection, REST page-number sources still need explicit non-replayable labeling or rejection, and vendor-backed external writeback still needs real remote lookup, compensation worker execution, persistent reconciliation queue, and approval flow beyond the current simulated/operator-resolve S53 slices.
- Ingestion: CSV upload and the application-level connector snapshot commit path are implemented. Connector ingest now includes the local/fake `ConnectorAdapter`, `RestPullConnectorAdapter` with auth/cursor pagination/rate-limit error handling, and `POST /api/connectors/webhooks/{connector_name}/{resource_name}` with timestamp-bound HMAC signature verification via `FOUNDRY_LITE_WEBHOOK_SIGNING_KEY` and append dataset commits. REST connector URLs default to public HTTP(S) targets only: localhost, private, link-local, internal, and metadata host targets are rejected unless `allow_private_network=True` is explicitly set for a local harness. REST cursor resume state is durable at the dataset transaction layer: committed connector syncs persist `dataset_transactions.metadata.connectorCursor`, the next sync uses that cursor by default when the caller does not provide one, and a failed page write cannot advance the durable cursor to the failed attempt's next page. Webhook duplicate delivery is idempotent for the same event id: `X-Foundry-Lite-Event-ID` or a deterministic payload-derived id is stored in `dataset_transactions.metadata.webhookEvent`, and a repeat delivery replays the existing committed version instead of appending another row/version. Webhook signatures include the request timestamp in the signed payload, and stale timestamps outside the replay window are rejected before append while leaving deny audit evidence. Stream archive ingest now includes a local/fake Kafka-compatible `StreamAdapter` proof through `DatasetWorkspace.archive_stream_events`, `StreamArchiveConfig`, APPEND dataset transactions, `dataset_transactions.metadata.streamCursor` checkpoint metadata, topic/partition/offset-derived event ids, `foundry_lite_stream_archive_lag_events`, and FAILED sync-run error trace evidence for writer failures. A failed archive write cannot advance the durable offset beyond the last committed dataset transaction. A production-compatible `KafkaStreamAdapter` and `foundry_lite_worker.stream_archive` one-shot worker now read broker-shaped messages through the same application boundary without direct dataset DB/storage writes; `StreamArchiveWorkerConfig.request_context` rejects a blank tenant id before worker writes can run, and `tests/integration/test_kafka_live_broker_stream_archive.py` proves the broker path against a live Kafka-compatible broker through Testcontainers. Debezium CDC now has both a fast adapter/archive proof, a live source proof, and an active-stack composition proof: `DebeziumPostgresStreamAdapter` normalizes PostgreSQL CDC insert/update/delete envelopes into `op`, `pk`, `before`, `after`, and `ordering`; `StreamArchiveConfig(schema_strategy="cdc_envelope_json")` archives those events into raw changelog datasets with previewable `pk_json` and `ordering_json` fields; `tests/integration/test_debezium_live_cdc.py` boots Kafka, logical-replication PostgreSQL, and Debezium Connect so `public.orders` insert/update/delete events are read from a real CDC topic and committed through the worker; `test_debezium_cdc_iceberg_s3_spark_archives_indexes_and_materializes_end_to_end` proves the archived CDC rows can be replayed from Iceberg-on-S3 into object indexing and `ops.order_current` with Spark active. CDC object indexing now supports optional ontology `backing.cdc` source mappings, `ObjectStore.index_cdc_events`, batch-rebuild-free CDC base patch upsert, `source_deleted` tombstones, duplicate/stale ordering skips, query/materialization tombstone exclusion consistency, fail-closed primary-key update rejection, `cdc_incremental` index runs, and `object.changed` outbox trigger evidence. CDC adapter read failures now create FAILED sync runs with Debezium adapter failure payloads in Operations, and active-stack archive writer failures leave no committed raw CDC dataset version. Production connector registry/config persistence, background retry workers, broader SaaS connector adapters, deployment-specific Redpanda profile packaging, and a continuously running CDC object-indexing worker remain future work.
- Object index reindexing: `ObjectStore.shadow_reindex` can build a shadow object index namespace, validate count/hash against the active baseline, replay action edit properties into the shadow rows, switch the active serving pointer through the object-type-level `object_index_versions.active_index_version` registry, and keep failed validation from replacing the previous active index. Row-level `index_version`/`is_active` flags still mark which object/link rows are served, but the active version is no longer inferred only from rows, so an object type with `0` rows can still preserve `index_run_*` as the version CDC/search workers should target. Shadow promotion uses a compare-and-swap check against the previously validated active version: if another switch wins first, the later switch fails instead of silently overwriting the pointer. Object Query cursor tokens now include the serving `active_index_version`; if a user fetches page 1 before shadow promotion and asks for page 2 after promotion, the cursor is rejected instead of mixing old/new index pages into duplicate or missing rows. The pointer row id is unique per insert attempt, while the conflict target remains the natural pointer key `tenant_id` plus `object_type_id`; this prevents concurrent first pointer creation from escaping the upsert path through a primary-key-only collision. A full reindex over the active index version is now idempotent: when the source dataset version, base patch, and merged properties are unchanged, the reindex skips the per-object update so it does not bump `object_version` or emit a spurious `object.changed` event, which keeps a held `expectedObjectVersion` valid after a replay. Object read, link read, object set membership, and materialization reads filter to active rows. `quality:shadow-reindex` plus `tests/integration/test_cdc_object_indexing.py` prove the local/fake, SQLite, and Postgres repository contracts, success/failure integration flows, the zero-row CDC follow-up case through `test_empty_shadow_reindex_persists_active_pointer_for_next_cdc_insert`, stale pointer rejection through `test_object_index_repository_contract_rejects_stale_shadow_pointer_switch`, active-index cursor rejection through `test_shadow_reindex_alias_switch_cursor_version_safe`, and a real PostgreSQL concurrent first-pointer race through `test_object_index_repository_contract_concurrent_first_pointer_switch_has_one_winner`. `VERIFY-ACTIVE-INDEX-POINTER-FLAKE` repeats that race `10` times against PostgreSQL to prove the old primary-key-vs-natural-key race is stable. API/CLI/Web operator controls for starting this shadow path are not yet exposed beyond the core facade.
- Search projection: `SearchIndexAdapter` now includes ontology-derived `SearchIndexMapping`, full-text `SearchQuery`, document id listing for drift checks, and local/fake/Elasticsearch-compatible adapter contracts. `ElasticsearchAdapter` is optional behind `foundry-lite[elasticsearch]` and the `FOUNDRY_LITE_SEARCH_PROFILE=elasticsearch` composition-root switch, while object store remains the source of truth. The API object query payload `search` field and `ObjectStore.query(..., search_text=...)` route searchable property full-text requests through the search adapter, then re-read active object records for masking and stale/orphan protection. Search hits expose the search projection version separately from the current object version, so a stale hit can be shown as stale while action entry still uses the fresh object-store state. Search adapter upserts are version guarded: a retry of an older `object.changed` document cannot overwrite a newer projection document in the local/fake/Elasticsearch-compatible profiles. `ObjectStore.consume_search_change` / `flite index consume-search-change` consume an `object.changed` payload for one object, and `ObjectStore.rebuild_search` / `flite index rebuild-search` rebuild the search projection and report `missingDocumentIds` / `orphanDocumentIds`. `quality:search-indexing` covers full-text search, object edit sync, rebuild count parity, orphan detection, and search-adapter failure fallback for get/basic filters; `quality:elasticsearch` now also includes live Testcontainers Elasticsearch round-trip/outage/concurrent-writer proof plus failed search projection evidence in Operations `indexRuns` and related audit. Managed cloud Elasticsearch packaging/ops runbooks remain future operational work.
- CSV upload size: default local limit is 50 MiB via `FOUNDRY_LITE_MAX_CSV_UPLOAD_BYTES`.
- Preconditions: `safeExpression` supports a small safe subset only:
  - `object.status in ['PENDING', 'REVIEW']`
  - `object.status == 'PENDING'`
- Writeback: `mock_erp_simulator` records simulated before-commit writeback rows. S53 adds partial unresolved-state slices: if a simulated external response is lost before local mutation, the action run and writeback row are marked `outcome_unknown`; if simulated external success is followed by local mutation failure, they are marked `compensation_required`; and if an operator later confirms remote success for an outcome-unknown writeback, `POST /api/operations/reconciliation/{writeback_id}/resolve` and the generated SDK can apply the original local object mutation and mark the action/writeback as `reconciled`. These paths retain `external_operation_id`, `idempotency_key`, `request_hash`, `remote_resource_id`, `last_observed_status`, `reconciliation_deadline`, and audit evidence, the compensation path also records `compensation_action_type`, same-key replay returns the existing unresolved run instead of blindly issuing another writeback, concurrent resolve has one winner, and sensitive writeback action parameters stay masked in Operations/audit surfaces. It still does not call an external ERP or provide real vendor remote lookup/compensation worker execution/persistent queue UI.
- Security default: API requests authenticate through an explicit `AuthProvider` port chosen by `FOUNDRY_LITE_AUTH_PROFILE` at the composition root. The local/default `HeaderTrustAuthProvider` still reads `X-Tenant-ID`/`X-User-ID`/`X-Roles` headers verbatim, but `FOUNDRY_LITE_RUNTIME_PROFILE=production` fails startup if `header-trust` or `demo` auth is selected. CLI and demo scripts continue to use an explicit demo admin context. S58A now has partial JWT/OIDC and SecretProvider slices: `JwtOidcAuthProvider` accepts `FOUNDRY_LITE_AUTH_PROFILE=jwt` or `oidc`, reads local OIDC discovery/JWKS JSON plus audience config, verifies RS256 bearer-token signature, issuer, audience, expiry, tenant, subject-or-service-account, and roles claims, tenant-scopes M2M tokens through `FOUNDRY_LITE_OIDC_SERVICE_ACCOUNT_CLAIM` (default `client_id`), blocks local revoked JWT IDs through `FOUNDRY_LITE_OIDC_REVOKED_JTIS_JSON`, and refreshes JWKS on an unknown `kid` while preserving cached keys for already-valid sessions. `SecretProvider`/`SecretValue` define a value-redacting port, `EnvSecretProvider` resolves `FOUNDRY_LITE_WEBHOOK_SIGNING_KEY` and `FOUNDRY_LITE_SECRET_<NAME>` aliases with hash-based local versions, `foundry.secret_provider` is wired through `CoreDependencies`, webhook signing no longer reads the secret directly through `os.getenv`, and `RestPullConnectorAdapter` re-resolves bearer/header credential secretRefs through `SecretProvider` on every snapshot call. Live OIDC discovery fetch, JWKS URI polling/TTL/key retirement, IdP introspection/refresh-token revocation, service-account registry/scope policy, IdP group-to-role policy, cloud/Vault secret managers, full connector workflow credential refresh, previous/current dual-read retry, and full secret rotation lifecycle remain future work.
- Security/Governance: `PolicyService` defines the v1 role matrix for `admin`, `data_engineer`, `ops_manager`, `viewer`, and `finance`; dataset/object reads, dataset writes, ontology activation, action execution, materialization, Operations reads/retry, and `object:explain` go through application-layer permission checkpoints. Sensitive data is no longer described as a one-off Order margin display rule: the composition root wires `PolicyService` to `OntologyRepository.active_property_classifications`, and classifications in `{"finance", "pii"}` become the source of truth for object-property and backing dataset-column sensitivity. Non-`finance`/non-`admin` callers get ontology-classified fields masked in object responses, link traversal, dataset preview rows, object query filter/orderBy, search mappings, dynamic object-set filters, and explain payloads, so masked values cannot be used for simple inference or leaked through `?explain=true`. `object:explain` itself is gated above plain object read; viewers do not receive the explain block, while operators receive masked base/edit properties. `PolicyService.mask_sensitive_properties` masks sensitive before/after refs before they are copied into durable action audit evidence, and `RuntimeService` recursively redacts sensitive keys from Operations summary/page/detail payloads, including S53 writeback reconciliation surfaces. Operations reads are operator-only through `operations:read:summary` and `operations:read:detail`, so viewers cannot browse raw run/audit/writeback/outbox rows. Evidence includes `test_policy_classification.py`, `test_dataset_preview_masks_sensitive_columns_for_unprivileged_roles`, `test_explain_does_not_bypass_property_masking`, `test_operations_read_is_operator_only_and_redacts_sensitive_params`, `test_action_audit_masks_sensitive_params`, `test_sensitive_writeback_payload_is_masked_in_audit`, and `test_masked_property_cannot_filter_sort_search`. Permission denials on mutation/activation/retry paths write `permission.denied` audit evidence with `decision=deny`. Tenant isolation is enforced at repository/API query boundaries, and PostgreSQL schema bootstrap applies RLS policies through `apply_postgres_rls` for tenant-scoped tables plus `dataset_schemas`. `set_postgres_tenant_context` sets transaction-local DB tenant context; `test_postgres_rls_hides_dataset_and_object_rows_between_tenants` proves a non-owner DB role sees only its own dataset/object rows, sees no rows without tenant context, and cannot insert another tenant's object row. `test_rls_tenant_context_reset_between_pooled_connections` forces the same PostgreSQL backend connection to be reused across tenant-demo, no-tenant, and tenant-other transactions and proves tenant context does not leak through the connection pool.
- Packaging/runtime profiles: optional vendor libraries for selectable profiles are declared as installable extras instead of dev-only conveniences. `tests/unit/test_packaging_extras.py` guards `foundry-lite[s3]`, `[iceberg]`, `[spark]`, `[temporal]`, `[elasticsearch]`, `[kafka]`, and `[postgres]` so production/profile installs cannot silently import-fail while the development environment passes.
- OSDK-lite: `scripts/generate_sdk_ts.py` reads the active supply-chain ontology and writes generated TypeScript SDK outputs to `packages/sdk-ts/src/generated.ts` plus the browser-ready `apps/web/generated-sdk.js`. Both outputs now export the same `SDK_CLIENT_SURFACE` generated from the shared ontology client surface, and `test_sdk_package_and_browser_outputs_share_client_surface` blocks object/action method drift between package and browser SDKs. The generated client exposes typed object get/query calls, typed action apply payloads, and helper functions for idempotency keys and expected object versions. SDK calls for frontend matrix rows marked `requiresIdempotencyKey` now require a caller-supplied `idempotencyKey`, and the browser SDK raises `MISSING_IDEMPOTENCY_KEY` before network I/O if a JavaScript caller omits it, so retry loops cannot accidentally generate a fresh mutation key per attempt. `examples/sdk-demo.ts` demonstrates a typed Order query and action apply flow, the Object Explorer now imports the browser SDK for object load and ApproveOrder action execution, and `quality:sdk-generated` blocks stale generated outputs when ontology apiName contracts drift.
- Object sets: static and dynamic saved object sets are implemented through core service, HTTP API, CLI, and Object Explorer list/create controls. Private owner visibility and temporary expiry are enforced. Static set member payloads are rebuilt through the object read/query boundary, so viewer reads still apply sensitive-property masking instead of exposing raw stored membership rows.
- Web Object Explorer: the current MVP web surface loads the committed `clean.orders` dataset version list and preview rows, validates ontology YAML without activating a new ontology version, displays YAML validation errors with the API request id, queries the pending Order list through the generated browser SDK/object query boundary, loads the O-1001 order detail, runs the `action_log` materialization through `POST /api/materializations/action_log/run`, previews the resulting `ops.action_log` dataset rows in the Dataset panel, and restores dataset/list/detail/link panels from the server on browser refresh after ApproveOrder.
- Source evidence and link navigation: `GET /api/objects/{object_type}/{object_id}?explain=true`, `flite object get <type> <id> --explain`, and the web Source Run control expose `sourceRunChain` links with `runType`, `runId`, status, relation, and operation detail endpoint path so an operator can move from object detail to source index/producer run detail without opening the database. Object links are exposed through `GET /api/objects/{object_type}/{object_id}/links/{link_type}`, `flite object links <type> <id> <linkType>`, and the Web Object Links panel; the current MVP proof shows the Order-to-Customer path from order O-1001 to customer C-100, and the same active OrderCustomer link row can be traversed in reverse so customer C-100 returns connected Order objects without storing duplicate reverse link state. If a stored link points at a target object that is no longer active, traversal returns the target object type/id with `targetMissing=true` and a `link_target_missing` warning instead of silently hiding the link.
- Operations surface: `flite operations runs --type/--status/--since/--until`, `GET /api/operations/runs?runType=&status=&since=&until=`, and the web Operations panel expose tenant-scoped sync, transform, index, action, materialization, outbox, DLQ, and audit rows with operator filters. `flite operations run <type> <id>`, `GET /api/operations/runs/{run_type}/{run_id}`, and the web Run Detail control return the selected row, status, normalized error message, correlation id, key references, a human-readable `investigation` summary with suggested next actions, related outbox/audit/object-edit/writeback rows, and lineage edges so an operator can investigate basic failures without opening the database. `flite transform retry <transform-run-id>`, `POST /api/operations/runs/transform/{run_id}/retry`, and the web Retry Failed Transform control retry a failed transform run with the original `input_versions` captured on that run, then write `transform.run.retry_requested` audit evidence linking the failed run to the new run. `flite index replay <object-type>`, `POST /api/operations/index/{object_type}/replay`, and the web Replay Index control rebuild an object index from the latest source dataset version and publish a fresh index run for operator follow-up. `flite index replay-run <index-run-id>`, `POST /api/operations/runs/index/{run_id}/replay`, and the web Replay Failed Index control replay a failed index run with its original `source_ref.dataset_version_id` and record `source_ref.replay_of_run_id` on the new run. `flite outbox retry <dead-letter-event-id>`, `POST /api/operations/dead-letter-events/{event_id}/retry`, and the web Retry DLQ control requeue an existing source outbox event from DLQ back to `pending`, reset attempts, remove the DLQ row, write audit evidence, and for `materialization.requested` payloads immediately run the named materialization with `materializationResult` evidence. Product workflow Operations now has a first S52 slice: `foundry.operations.start_connector_sync_workflow(...)`, `POST /api/operations/workflows/connector-sync/start`, and `GET /api/operations/workflows/{workflow_run_id}` start and inspect `ConnectorSyncWorkflow` with a stable idempotency-key workflow id, `ProductWorkflowRun` payload, and audit evidence linking `workflowRunId` to `foundryRunId`. Broader failed sync/action/materialization-run replay workflows and full product workflow recovery actions are still future work.
- Transform/materialization version pinning: transform runs store exact `input_versions` during planning and execute SQL against those version files, while materialization commits emit `materialization.completed` outbox payloads with the committed `versionId`. `test_downstream_transform_consumes_materialized_version_id_not_latest` proves a newer `ops.order_current` materialization becoming latest before SQL execution does not change the downstream transform input.
- Sprint 36A operational hardening: concurrent same-key action idempotency now turns database unique races into replay through `ActionRepository.insert_action_run_or_get_existing`, and same-key/different-request reuse is blocked by a stored `request_fingerprint` plus `action.run.idempotency_conflict` audit evidence. Dataset commit locks the dataset row before version allocation, maps duplicate metadata inserts to `DatasetVersionConflictError`, and removes already-promoted artifacts through `DatasetStorageAdapter.delete_committed_version` while preserving `orphan_cleanup` evidence. The split-brain regression suite now also proves that this evidence survives into the FAILED sync run and that missing committed manifest/data files are reported as `committed_version_storage_missing` instead of generic file errors. Object Query delegates tenant/object type/filter/order/cursor/limit to `ObjectReadRepository.query_active_object_rows`, uses signed opaque `oqc1.` cursor tokens with query-shape checksum and `active_index_version` binding, rejects raw object-id cursors and tampered cursor payloads, rejects cursor reuse after a shadow index switch, validates missing filter/order properties in `ObjectQueryService`, applies ontology/property metadata casts for DB JSON sort/filter expressions, adds `object_id` as the stable tie-breaker, and caps page size at 500; fake, SQLite, and Postgres repository contract tests cover numeric string sort/cursor parity. Dynamic Object Set membership follows the same cursor paging. Operations run lists use `RuntimeRepository.query_run_rows` with status/date filters, `created_at` or `failed_at` plus run id keyset cursor tokens, API/CLI `limit`/`cursor`, and a max page size of 500.
- Demo execution: `pnpm demo:supply-chain` uses an isolated `.foundry-lite-demo/` home and fresh execution by default when `FOUNDRY_LITE_HOME` is not explicitly set. This keeps the one-command demo repeatable and prevents old local action/object state from blocking the closed loop. Explicit `FOUNDRY_LITE_HOME` runs are preserved unless the caller passes `demo run-supply-chain --fresh`.
- CLI: current implementation uses Python `argparse`; Typer is not installed in the current runtime.
- Worker: `apps/worker` now includes the stream archive one-shot composition root for Kafka/Redpanda ingestion, plus a bounded continuous stream archive mode that repeatedly calls the existing dataset transaction/checkpoint boundary until configured empty polls, max batches, or a stop callback ends the loop. `ConnectorSyncWorkflow` is registered in the Temporal workflow definitions and is covered by the time-skipping integration ratchet, but managed worker deployment/operations and real connector activity execution are still future work. CDC object-indexer daemon mode, lease/fencing, rebalance revoke protection, and commit-unknown reconciliation are also still future work.
- Stale transaction recovery: `DatasetWorkspace.abort_stale_open_transactions` delegates to `DatasetRecoveryService.abort_stale_open_transactions`, which lists OPEN `dataset_transactions` created before an operator-provided cutoff through `DatasetTransactionRepository.list_open_transactions`, cleans up their staging artifacts, and marks them `ABORTED` with `abortedBy=watchdog` metadata plus `dataset.transaction.aborted_by_watchdog` audit evidence. `test_failed_upload_oom_leaves_recoverable_aborted_or_stale_open_tx` proves an OOM-abandoned OPEN transaction is recovered while a freshly opened transaction is left untouched. The current cutoff is wall-clock-age based; a heartbeat/lease-driven detector remains a future hardening.
- Migrations: SQLAlchemy `metadata.create_all` remains the local bootstrap path, and the branch now includes an Alembic baseline migration plus post-baseline migrations for Record DLQ, dataset layout policy, and dataset quality evidence. `tests/integration/test_migrations.py` applies `alembic upgrade head` to a fresh DB and proves the migrated tables/columns match `schema.metadata`. `check_schema_revision_guard.py` still fingerprints `schema.py` metadata and blocks DB table/column/unique-constraint changes unless the latest `infra/schema_revisions` snapshot is updated in the same change. S55 adds `quality:schema-migrations` through `scripts/quality/check_schema_migrations.py`, which blocks multi-head Alembic history, revision filename mismatch, and destructive `downgrade()` schema operations so rollback policy is forward-fix/restore-runbook-first. The same gate now requires `migration_phase` and `release_compatibility` metadata, allows only compatible expand-phase schema operations in the `old_and_new_app` window, rejects new `NOT NULL` columns without a default for old writers, and blocks contract-phase cleanup until old-writer rejection/release-window proof exists. S55 also adds the dedicated `db:migrate` runner in `scripts/operations/run_migrations.py`: it passes one locked DB connection into Alembic, uses PostgreSQL advisory lock semantics for PostgreSQL targets and SQLite `BEGIN IMMEDIATE` for local proof, writes password-masked migration run evidence for success, lock-busy, and failure outcomes, and `quality:schema-migration-runner` proves concurrent local migration jobs have one winner while app/worker startup does not call Alembic directly. `quality:schema-evolution` now covers the Dataset side of S55 partially: `schema_evolution.py` classifies rename/drop/type narrowing/primary-key change as blocked, allows numeric widening with transaction metadata warning, records deprecated-field consumer warnings, and creates deterministic backfill progress metadata for schema changes that need later backfill execution. `quality:ontology-migrations` now covers the Ontology side partially: `ontology_migration.py` blocks consumer/SDK-breaking property, object type, link type, and action parameter changes before activation, records deprecated-property warnings, emits deterministic object reindex plans in activation audit/outbox evidence, and keeps generated SDK drift in the same gate. Live PostgreSQL contention proof, full old/new app deployment-window proof, multi-step production upgrade workflows, rollback/restore runbooks, actual ontology migration executors/reindex workers, real backfill worker/progress APIs, and deployment rollback tests remain future work.
- Application structure: `FoundryLite` is the platform root, exposing one thin facade per bounded context — `datasets` (`DatasetWorkspace`), `transforms` (`TransformPipeline`), `ontology` (`OntologyRegistry`), `objects` (`ObjectStore`), `actions` (`ActionGateway`), `materialization` (`MaterializationRunner`), `operations` (`OperationsConsole`), and `demo` (`SupplyChainDemo`) — while lifecycle (`bootstrap`/`reset`) stays on the root. Each sub-facade forwards to the focused service modules behind it (Dataset, Transform, Ontology, Object, Action, Materialization, runtime event, and demo orchestration). Each service declares the exact `CoreDependencies` fields it directly uses through `required_dependencies` and the exact service collaborators it directly calls through `required_collaborators`; cross-service calls use explicit collaborator attributes such as `runtime_service` or `dataset_registry_service`. CI blocks application modules above 500 lines.
- Scale foundation status: Sprint 02A boundary extraction is now in place for the local slice. `DatasetStorageAdapter` is a real port with local, fake-storage, and S3-compatible adapters; `S3DatasetStorageAdapter` is selected by the `s3-storage` adapter profile, keeps `boto3` behind the optional `foundry-lite[s3]` extra, downloads committed objects through the port for preview/transform compatibility, and is covered by `quality:s3-storage` against MinIO/Testcontainers. `WorkflowAdapter`, `StreamAdapter`, `SearchIndexAdapter`, and `ConnectorAdapter` now define vendor-neutral Scale Foundation swap points with local/fake in-memory adapters, local-runtime wiring, and contract tests. `StreamAdapter` also backs the stream archive writer slice, where stream events are written into raw archive dataset versions through the normal transaction protocol rather than bypassing dataset storage. `KafkaStreamAdapter` is the first production-compatible stream adapter and is wired through the worker composition root instead of application services importing broker SDKs; the Kafka SDK is exposed as the optional `foundry-lite[kafka]` extra so non-Kafka deployments do not inherit the vendor client. `DebeziumPostgresStreamAdapter` is a wrapper over the stream port, so Debezium-specific payload parsing stays in infrastructure while application services only see the standard CDC envelope. `ElasticsearchAdapter` is now the production-compatible search adapter and remains behind the search port so application services do not import Elasticsearch SDKs. Live Kafka-compatible broker proof is covered by `tests/integration/test_kafka_live_broker_stream_archive.py`; live Elasticsearch cluster proof is covered by `tests/integration/test_elasticsearch_live_cluster.py` and the durable operator-evidence path is covered by `tests/integration/test_search_indexing.py`. S52 adds a partial product-workflow use of the workflow port: `ConnectorSyncWorkflow` starts through the same Operations contract on local/fake and Temporal profiles, and audit evidence links the workflow run to a Foundry operations run. `MetadataRepository` now owns schema bootstrap/reset/default tenant-user DB writes and participates in SQLite/fake/Postgres repository contract coverage, `DatasetRepository` owns dataset registry create/find DB reads/writes, `DatasetTransactionRepository` owns dataset transaction/version/file DB state changes plus best-effort run failure updates, `DatasetVersionRepository` owns committed version/schema DB reads, `RuntimeRepository` owns audit/outbox/lineage/list-runs DB boundaries, `ComputeAdapter` owns CSV/Parquet/SQL transform/health-check execution behind DuckDB local and fake compute adapters, `ObjectReadRepository` owns object record/link read DB boundaries, `ObjectIndexRepository` owns object indexing run/object record conflict/link write DB boundaries, `ObjectSetRepository` owns object set row/membership metadata DB boundaries, `ActionRepository` owns action run/writeback/object edit writes, and `OntologyRepository` owns ontology version/object/property/link/action type metadata DB boundaries. Every current concrete adapter profile now exposes `AdapterFailureContract`, which freezes failure `kind`, `is_retryable`, timeout, idempotency-key, and operator-facing message semantics for future scale adapters. Production connector implementations, managed Temporal worker operations, and managed cloud Elasticsearch operations remain future work.
- Infra boundary gates: CI now blocks domain concrete infra imports, application concrete infra imports above the current baseline `0`, API router direct repository/DB transaction access, repository domain-error/business-rule ownership, repository direct transaction commit/rollback, application ports without contract tests, current-state docs that reference missing source paths/scripts/Python symbols, scale SDK imports in domain/application, Tach module-DAG violations, undeclared/unused service dependencies and collaborators, hidden service attribute access, explicit collaborator call graph cycles/depth/fan-out regressions, and any return of `core._...` private facade tests.
- Release-gate hardening: CodeQL P7 is GitHub Actions only, not part of local `pnpm ci:gate`, because fresh Python DB builds take minutes. The CodeQL workflow now fails on SARIF findings instead of merely uploading alerts. GitHub CI now runs the same `ci_gate.sh` evidence as parallel `static`, `coverage`, `flaky`, `runtime`, and `e2e` lanes, then preserves the branch-protected `quality-gate` check as an aggregate job that fails unless every lane succeeds. Local `pnpm ci:gate` remains a full serial release gate. Function-length G3 now enforces a 40-line hard limit with zero baseline application functions. Boolean naming C1 blocks unclear boolean argument and annotated-field names in `libs`, `apps`, and `scripts`, while leaving full boolean-return predicate naming as remaining partial coverage. Dict-any G4 freezes the current application function-signature `dict[str, Any]` count at 0 total, with layer baselines for core facade 0, ports 0, helpers 0, and services 0, so new application function signatures cannot reintroduce schema drift through untyped dictionaries. Query side-effect guard blocks read-like service entrypoints from reaching repository writes, audit/outbox/lineage writes, file writes, write adapters, or mutation collaborators. Strategy/specification testability guard requires filter/precondition evaluator modules and future `*Strategy`/`*Specification` rule modules to have direct unit/property test imports instead of being tested only through API/Core flows. Integration scenario marker G15 requires the seven engineering-guide §11.4 MVP release integration scenarios to be marked by pytest scenario evidence before the release gate can pass, with the closed-loop repeatability smoke tracked as additional evidence. The Testcontainers closed-loop contract now runs connector snapshot sync into dataset commit, transform, index, action, and materialization on PostgreSQL-backed repositories. Root-cause patch prevention blocks fix/bug/patch/regression commits without same-commit `tests/` changes and requires PR descriptions to include Root Cause, Impact, and Regression Test sections. Infra Ratchet discipline is now a static CI gate through `check_infra_ratchet.py`: infrastructure must be added one family at a time, and active ratchets must carry adapter-contract, normal-path, failure-injection, concurrency-race, retry-idempotency, partial-success, recovery-cleanup, operator-evidence, and docs-sync proof expectations before the next infrastructure advances. The companion `quality:checklist-evidence` gate runs `check_checklist_evidence.py`, so every checked `test_*` name in the tricky failure checklist must be collected by pytest before CI accepts the documentation as true evidence. `quality:infra-tricky-matrix` runs `check_infra_tricky_matrix.py`, so each active infra family and active composition stack must pull related tricky checklist item ids into proof classes, collectable pytest tests, and CI commands. `quality:s3-storage` is now part of the runtime lane and proves the first MinIO/S3 storage ratchet before Iceberg advances to implementation. Tenant write guard S1 blocks tenant-scoped SQLAlchemy insert/update/delete statements that omit `tenant_id` values or `tenant_id` where clauses. Schema revision guard fingerprints the current SQLAlchemy metadata and blocks DB table/column/unique-constraint drift unless `infra/schema_revisions` is updated with the same change. Audit-on-mutation G2 blocks public application service mutation entrypoints that can reach repository writes without a reachable audit/outbox call tree. Transaction outbox/audit pair G11 blocks transaction-scoped writes that lack audit/outbox proof in the same transaction call tree, and action idempotency G12 blocks Action API/Core/Service/schema regressions that would make `Idempotency-Key` optional, bypass existing action_run replay, skip request fingerprint comparison, or omit idempotency-conflict audit evidence. Error response request-id G7 blocks FastAPI error detail and `_handle_error` usage that would omit the user-facing `request_id`. Log trace-key G5 blocks direct logger calls or `log_event` calls that would write operational logs without `request_id`, `tenant_id`, or run id-family correlation fields. Metrics exposure blocks the Prometheus payload from losing the eight required operational metrics for dataset commits, transform runs, action apply, object query, outbox lag, failed runs, DLQ size, and stream archive lag. Adapter failure taxonomy blocks new concrete adapters unless they expose typed failure kind, retryability, timeout, idempotency-key, and operator-message semantics. Runtime audit-count G17 checks the supply-chain demo DB so high-level mutation evidence and durable `audit_events` rows match exactly. Outbox consistency G18 checks the same demo DB so event-propagated state changes and durable `outbox_events` rows match by event type, aggregate, idempotency key, and correlation key. MVP data-correctness validation checks the same demo DB for expected dataset row counts, object primary-key uniqueness, stable Order reindex source hashes on a cloned store, and exactly one succeeded ApproveOrder action for the demo idempotency key. MVP performance smoke runs a fast CI profile through CSV ingest, object index, object query, and no-writeback action apply, while `quality:mvp-performance-release-100k` and `quality:mvp-performance-release-1m` keep the larger Sprint 36 measurements reproducible without slowing every local gate. OpenLineage P8 runs after the supply-chain demo smoke and verifies successful transform `input_versions`/`output_version_id` against `lineage_edges`, while blocking missing, duplicate, non-version, or failed-run lineage and writing OpenLineage-compatible RunEvent artifacts. Trace continuity G16 runs the demo under an in-memory OpenTelemetry provider and verifies synthetic request, required service, and SQLAlchemy DB spans share one trace with the expected `request_id` on service spans. Adapter error trace-key validation forces an exploding compute adapter and blocks FAILED run error payloads that omit `tenant_id`, `actor_user_id`, `request_id`, `run_id`, `correlation_id`, or `adapter`. Failed mutation state validation forces an exploding CSV adapter and blocks any mismatch between FAILED sync run, ABORTED dataset transaction, abort audit, error trace, and absence of committed dataset versions. Flaky detector G19 repeats the pytest-randomly + pytest-xdist command three times and blocks release evidence if any run fails or the passing summary changes. gitleaks remains fast enough for `ci_gate.sh`; local missing-tool state warns, but CI/release evidence fails if gitleaks is missing. Test `sleep` calls are blocked by `check_no_test_sleep.py`, `# pragma: no cover` exclusions are blocked at baseline `0` by `check_pragma_no_cover_budget.py`, and `check_tier_coverage_by_layer.py` prevents average coverage from hiding weak domain/application/infrastructure/API/CLI/worker layers. AST-grep now blocks `FoundryLite` from reintroducing hidden `__getattr__`/`__setattr__` facade magic.
- Typed JSON boundary progress: Action and runtime repository ports now use named `Mapping[str, object]` aliases for writeback payloads, object edit patches/properties, audit refs, and outbox payloads. API and CLI JSON object entrypoints no longer expose dict[str, Any]; request params, object-set filters, and CLI JSON args use `dict[str, object]` at the edge. Safe-expression and query-filter helpers no longer import the broad typing escape hatch; they accept JSON-ready `object` values and validate string/list/object shapes before evaluating rules. `ActionService` and `action_helpers` now use `ActionTypeRow`, `ObjectRecordRow`, and `TransactionContext` for the action apply path, with named action repository payload aliases for object patches and writebacks. `ObjectIndexingService` now uses typed tabular rows, `ObjectRecordRow`, typed object property maps, `IndexRunError`, `ObjectConflictRecord`, and typed collaborator protocols for the object indexing path. `OntologyService` now uses `TransactionContext`, typed collaborator protocols, and YAML helper contracts for dataset lookup, dataset version/schema lookup, runtime audit/outbox boundaries, action definition persistence, and schema-column validation. `ObjectSetsService` now uses `TransactionContext`, typed collaborator protocols, and object-validated filter/static-id inputs for object set creation and membership reads. `TransformService` and `MaterializationService` now use typed collaborator protocols plus `TransactionContext` for transform run, lineage, materialization commit, and materialized runtime-row handling. `RuntimeService` now uses `TransactionContext`, runtime row mappings, and `FoundryLiteError` for audit/outbox/lineage/error-payload boundaries. `ObjectRecordsService`, `ObjectLinksService`, and `ObjectQueryService` now use object record/link rows, typed object-store collaborator protocols, `TransactionContext`, and explicit property sort keys for read/link boundaries. Dataset services and demo orchestration now use typed collaborator protocols, `CoreDependencies`/`CoreService` use `TransactionManager` for the transaction-opening engine, and primitives use `object` JSON boundaries. `check_application_any_budget.py` freezes broad Any usage at 0 for `libs/foundry_lite/application`, `apps/api`, `apps/cli`, and `apps/worker`, so #28's application/app boundary is now complete. Action mutation definitions require explicit `type` and `property` keys. SQLAlchemy adapters materialize mappings with `dict(...)` at the database boundary, reducing untyped JSON exposure without changing persisted JSON behavior.
- Transaction boundary: `TransactionContext` is now an explicit opaque Protocol in `application/ports`. All repository ports take `transaction: TransactionContext` instead of `transaction: Any`, including failed-run abort paths, so future scale adapters (PostgreSQL test containers, in-memory fakes, transactional Kafka outbox writers) can supply their own handle types without changing repository signatures. Dataset failed-run abort now returns whether an OPEN transaction was actually moved to ABORTED, letting the application layer write abort audit evidence exactly once inside the same transaction.
- Concrete infra imports in `application/`: now `0`. Every service module talks to repository ports only. The remaining concrete SQLAlchemy access lives behind `infrastructure/repositories/*`.

## Still Targeted, Not Yet Implemented

- S46/S47/S48/S49/S50/S51/S52/S53/S54/S55/S56/S57/S58/S60 data-platform expansion progress: [Data Platform Expansion Sprint Plan](./data-platform-expansion-sprint-plan-ko.md) is the post-MVP execution plan, and S46 provides [Data Engineering Pattern Matrix](./data-engineering-pattern-matrix.json), `quality:semantic-doc-consistency`, and `quality:data-pattern-matrix`. These gates keep README/current-status wording, deferred/partial data-pattern claims, active-covered proof levels, future tests, and active infra references synchronized as S47+ product features land. S47 Record DLQ is now partial: stream CDC archive can quarantine invalid CDC envelope records into tenant-scoped `dead_letter_records`, keep valid records flowing into the dataset commit, write `dead_letter_record.quarantined` audit evidence, and fail closed when DLQ storage fails. The `StreamArchiveConfig` `max_record_error_ratio` field fails a stream/source batch closed when bad-record ratio exceeds the source policy, while default `fail_closed_error_kinds` make identity/primary-key and ordering errors fail closed instead of silently archiving a mixed-validity batch. Operations API and typed generated SDK calls can list/detail record DLQ rows, replay one or many records with an `Idempotency-Key`, return per-item bulk replay `status`/`failedItems`/`succeededCount`/`failedCount` for partial success, return the existing replay result for the same key, persist downstream backfill plan metadata, discard records, and write replay/discard/bulk-item-failure audit evidence. Valid stored DLQ payloads now replay through the normal APPEND dataset transaction into the original raw stream archive dataset, returning `replayDatasetVersionId` and `rowCount`; invalid replay payloads stay quarantined with `replayStatus=FAILED`, an error payload, and `dead_letter_record.replay_failed` audit evidence. A PostgreSQL concurrent replay request proof keeps one replay run per record instead of last-writer-wins overwrite. Web Operations UI now exposes record DLQ status filtering, list/detail, impact preview, single replay, bulk replay, discard, and open/closed/top-error summary metrics. S48 Late Data + Watermark is now partial for stream/source archive: `StreamArchiveConfig` declares source-specific `event_time_field`, `source_time_field`, named `time_zone`, `allowed_lateness_seconds`, `too_late_after_seconds`, and `clock_skew_seconds`; committed stream archive rows keep `event_time`, `source_time`, `ingested_at`, `processed_at`, `late_data_status`, and `event_time_lag_seconds`; too-late records enter Record DLQ with `TOO_LATE` error evidence; `dataset_transactions.metadata.lateDataWatermark` keeps a matching partition/source watermark from moving backward while ignoring unrelated partition/source watermarks so slow partitions are not silently treated as stale; duplicate delivery after a committed late event resumes from the durable offset instead of creating a second dataset version; late stream commits persist `lateDataSummary` plus `lateDataReprocessingPlan`, which Operations run detail exposes with the underlying dataset transaction; materialization commits now persist `materializationDetail.watermark` plus `reopen.reason=late_data_reprocess` when a `LATE_REQUIRES_REPROCESS` CDC index run causes the next closed output to be regenerated; object explain and materialization detail expose `lateDataBadge`; Operations run detail exposes `downstreamImpact` roots, badges, and affected run links for the regenerated materialization; and stale late CDC deletes are skipped so they cannot remove or resurrect the current active object. `quality:late-data` and `quality:watermark` are connected to the runtime lane. S49 Multi-file Dataset + Partitioning is now partial for the read/preview path: `DatasetStorageAdapter.data_file_paths()` resolves every manifest-listed data file in order, `foundry.datasets.preview(...)` reads multi-file manifests without directory/bucket listing, single-file manifests remain readable, dataset rows can declare `partition_spec`, `sort_order`, and `target_file_size_bytes`, and `quality:multi-file-dataset` plus `quality:partition-pruning` are connected to the runtime lane. Local/fake/S3 read/preview partition filters reduce actual manifest file reads by matching file-level `partition_values`; Iceberg keeps its snapshot materialization contract and only skips reads when no manifest file matches. S50 Iceberg Maintenance is now partial for planning/evidence: Operations can create a dry-run maintenance plan through `foundry.operations.plan_iceberg_maintenance(...)`, `GET /api/operations/maintenance/iceberg`, and `POST /api/operations/maintenance/iceberg/{dataset}/plan`; the plan reports compaction candidates, orphan snapshots, protected DB committed-version snapshots, retained snapshots, and audit evidence, with `quality:iceberg-maintenance` connected to the runtime lane. S51 is partial for bounded continuous stream archive worker looping through the existing transaction/checkpoint boundary, with worker CLI env/schema/tenant configuration failures returned as `CONFIGURATION_ERROR` JSON payloads instead of stacktraces. S52 is partial for product workflow control-plane integration: `ConnectorSyncWorkflow` starts and is queried through Operations/API/SDK on local/fake/Temporal profiles, with `quality:temporal-engine-integration` proving stable idempotency-key workflow ids, shared `ProductWorkflowRun` shape, and audit linkage. S54 is partial for data quality contract evidence: dataset quality check results now persist `checked_manifest_hash`, `validated_against_schema_version_id`, and `validated_against_schema_version`, and `quality:data-contracts` proves the dataset commit path plus repository contract preserve those references. The same gate proves successful check rows are stored as `PASS`, warning severity failures stay visible as non-blocking `WARN`, staged candidate tamper is rejected before storage commit, commit-time hard failures surface as `BLOCK_COMMIT`, and row-level `not_null`/`unique` quarantine checks route failed records to Record DLQ before committing a revalidated clean candidate. Operations run detail now exposes the same transaction-scoped quality summary, schema references, checked manifest hashes, check results, and data-quality quarantine failed-row samples. S55 is partial for Alembic migration safety, runner safety, expand-contract phase guard, operator evidence, Dataset schema evolution evidence, and Ontology migration plan evidence: `quality:schema-migrations` proves single-headed history and forward-fix-only downgrades, `quality:schema-migration-runner` proves the dedicated `db:migrate` path uses a singleton DB lock and app/worker startup does not run Alembic directly, `quality:schema-evolution` proves widening metadata plus deterministic backfill-progress skeletons, and `quality:ontology-migrations` proves activation-time ontology breaking-change blocking, object reindex plan evidence, and generated SDK compatibility checks. S56 is partial for read-only observability detector evidence: `quality:observability-detectors` and `quality:slo-contracts` prove missing-data, lag, skew, cooldown dedupe, SLO evidence, API/SDK surface, and runtime-lane wiring. S57 is partial for backup/restore preflight and restore-mode lockout evidence: `quality:backup-restore` proves `BackupRestoreService` can inventory active dataset committed versions, validate committed storage manifests and data-file byte/hash evidence, mark DB/storage point mismatches as `blocked`, report active object index pointers, action/outbox/audit high-watermarks, Temporal restore strategy, search projection rebuild markers, API/SDK surface, restore-mode start/status, idempotent same-restore replay, serving traffic closed status, outbox retry/reprocess lockout, and runtime-lane wiring. Multi-part atomic commit, transform predicate pushdown, high-cardinality partition warnings, Iceberg file-level pruning, actual `rewrite_data_files`, snapshot expiration, orphan cleanup execution, transform-level Record DLQ policy, CDC object-indexer daemon/rebalance fencing, full connector-sync activity execution, full DataContract CRUD, owner notification, dedicated failed-row sample UI, quality history/trend, production DB schema race proof, live PostgreSQL migration-lock contention proof, full old/new runtime window proof, actual ontology migration executors/reindex workers, real backfill execution/progress APIs, full backup artifact creation, restore-mode write traffic enforcement, outbox publisher pause/resume execution, post-restore closed-loop validation, stored incident lifecycle, notification delivery, SLO dashboard/source-health timeline UI, broker-offset/REST-cursor lag adapters, object-key skew, and persistent threshold registry remain future.
- S55 migration phase guard is also active inside `quality:schema-migrations`: migrations must declare `migration_phase` and `release_compatibility`, expand operations are checked for old-writer-compatible shape, default-less new `NOT NULL` columns are blocked, phase/window mismatch is rejected, and contract cleanup remains fail-closed until old-writer/release-window proof exists. `db:migrate` also writes password-masked operator evidence for failed migration attempts. `quality:schema-evolution` adds Dataset schema-change impact evidence and deterministic backfill-progress skeletons, and `quality:ontology-migrations` adds activation-time ontology migration plan evidence plus SDK compatibility checks, but full ontology migration executor, rolling-deploy runtime proof, real backfill execution, and restore runbook rehearsal remain future.
- S56 Proactive Observability + SLO is partial for read-only detector evidence: `observability_detectors.py` evaluates versioned detector configs against the current `RuntimeRunSnapshot` without mutating runtime rows; `quality:observability-detectors` proves missing-data flow interruption with zero failed runs, event-time vs processing-time lag evidence, seasonality-aware skew detection, cooldown dedupe, API/SDK surface, and runtime-lane wiring; `quality:slo-contracts` proves an SLO breach carries run and dataset references. Stored incident lifecycle, notification delivery, SLO dashboard/source-health timeline UI, broker-offset lag, REST cursor lag, tenant/object-key skew, object-index/workflow-action SLO adapters, and persistent threshold registry remain future.
- S57 Backup/Restore Commit-point Ratchet is partial for preflight, restore-mode lockout, and operator resume approval evidence: `backup_restore_service.py` builds an Operations report that compares metadata DB committed dataset versions with committed storage manifests/data files, reports missing/corrupt versions as `blocked`, captures active object index pointers, action/outbox/audit/materialization high-watermarks, Temporal restore strategy, search projection rebuild markers, and writes `backup_restore.preflight_reported` audit evidence. The same service now starts and reads durable restore-mode status through Operations/API/SDK, records `is_serving_traffic_open=false`, keeps `is_outbox_publisher_paused=true`, blocks outbox dead-letter retry/materialization reprocess entry while restore mode is active, returns the existing status for the same `restoreId` instead of duplicating restore-mode audit evidence, and approves `resume_approved` only after dataset inventory, object index pointer, action run, and materialization run evidence exist. Actual backup artifact creation, platform-wide write traffic gate enforcement, real outbox publisher daemon pause/resume execution, automatic restore smoke execution, and Kubernetes packaging remain future.
- S58A OIDC/JWT + Secret Provider is partial for local JWT/OIDC verification and the secret-provider boundary: `JwtOidcAuthProvider` validates bearer tokens against local OIDC discovery/JWKS JSON, issuer, audience, expiry, RS256 signature, tenant claim, subject-or-service-account claim, roles claim, and local revoked `jti` denylist, with JWKS refresh-on-unknown-`kid` and key-cache retention during rotation. M2M tokens without `sub` can authenticate only through a configured service-account claim and still require a tenant claim. `SecretProvider` and `EnvSecretProvider` now resolve local environment-backed secrets through `CoreDependencies`, expose redacted `SecretValue` operator evidence, include adapter failure taxonomy coverage, power the webhook signing-key path through `foundry.secret_provider`, record missing webhook signing-key failures as `webhook.secret_resolution_failed` audit evidence with a redacted error payload and request id, and let `RestPullConnectorAdapter` re-resolve bearer/header credential secretRefs on each snapshot so a rotated local env secret is used by the next connector call. Live OIDC discovery fetch, JWKS URI polling/TTL/key retirement, IdP introspection/refresh-token revocation, service-account registry/scope policy, IdP group-to-role policy, Vault/cloud secret manager adapters, full connector workflow credential refresh, previous/current dual-read retry, and production-grade rotation policy remain future.
- S58B Anonymization/Pseudonymization is partial for a versioned privacy transform core: `PrivacyTransformPlan`, `PrivacyFieldRule`, and `transform_privacy_rows` can pseudonymize identifiers with tenant-scoped HMAC output, anonymize configured fields as `***ANONYMIZED***`, redact local email/SSN/US-phone samples in text as `***REDACTED_PII***`, and return replayable lineage metadata with plan name/version, rule count, and row count. `PrivacyFieldRule(is_reversible=True)` now requires an explicit protected mapping store; `InMemoryProtectedPrivacyMappingStore` keeps pseudonym-to-original mappings outside transformed rows and lineage, and exposes only redacted mapping evidence with an original-value hash. `PrivacyReplicationPolicy` and `validate_privacy_replication_policy` now block production-to-staging/analytics/AI replication when no privacy plan exists or when configured sensitive fields are missing/passthrough, and allowed transforms attach source/target environment and plan-version evidence to lineage. `PrivacyDatasetRef` can link a source dataset version to the anonymized target dataset version, and `build_privacy_openlineage_event` emits a replay-stable OpenLineage-compatible privacy RunEvent artifact without raw PII values. `quality:privacy` covers same-identifier determinism within a tenant scope, cross-tenant pseudonym separation, absence of raw PII samples in transformed rows, replay-stable plan lineage, protected-store enforcement for reversible mappings, raw-value absence from redacted evidence, production-to-nonprod replication policy enforcement, anonymized dataset version lineage, and raw-value-free privacy OpenLineage event artifacts. Durable environment replication workflow, durable/encrypted protected mapping backend, ontology-driven privacy policy registry, runtime DB `lineage_edges`/outbox/OpenLineage transport integration, and erasure lifecycle remain future.
- S58C Right-to-Erasure Lifecycle is partial for a local erasure manifest proof: `ErasureRequest` records tenant, request id, subject kind/value, requester, idempotency key, reason, and requested-at while exposing only `subjectHash` plus redacted evidence; `resolve_erasure_subject` resolves candidate records through tenant-scoped identity fields and ignores same-subject records from other tenants; `ErasureManifest` turns resolved dataset/object/search/materialization/DLQ/backup surfaces into stable, idempotent actions such as `tombstone_object`, `remove_search_document`, `rebuild_materialization`, `redact_dead_letter_payload`, and `defer_backup_expiration`; `ErasureRetentionPolicy` records backup retention and crypto-shredding key references; every manifest includes `minimize_audit_evidence`; and `is_erased_resource` gives search rebuilds a local exclusion proof so an erased search document is not treated as resurrectable. `quality:erasure` covers serving-surface manifest action planning without raw subject values, tenant-scoped resolution, idempotent replay, backup-retention pending state, and search rebuild exclusion. Durable erasure request storage, API/workflow entrypoints, actual Object Store tombstone writes, SearchAdapter deletes, materialization recompute, DLQ payload redaction, backup manifest rewrite, KMS/cloud crypto-shredding, and audit compaction executors remain future.
- S60 Fine-grained Lineage + AI Evidence is partial for property-level object explain and immutable AI evidence references: object explain now includes `propertyLineage` entries with property name, value source, source dataset version, source object version, source column, source hash, property version, and masking state, and the generated SDK type includes that field. `EvidenceReference`, `EvidenceSourceSpan`, `build_insight_claim_payload`, `build_llm_extraction_evidence`, and `revise_evidence_reference` prove insight claims require evidence object ids, LLM extraction evidence pins source dataset/object version plus extractor/model/prompt versions and parameter hash, reprocessing keeps the old evidence id while creating a new revision, and masked source spans do not expose raw quotes. `quality:ai-evidence` covers those paths. Output-column lineage, link source-key mapping, durable AI evidence tables, real LLM extraction executors, insight evidence viewer UI, model-change diff UI, and AI action execution policy remain future.
- S61 Frontend Foundation + Generated SDK is partial but now locks the current frontend backend surface more strongly than the first request-wrapper slice: generated TypeScript/browser outputs expose typed frontend API errors, request/context helpers, retry/backoff, cursor pagination, duplicate-action lock, stale-version conflict classification, permission-denied classification, generated object/action methods, and named SDK namespaces for system health, dataset list/versions/preview/inspect, ontology catalog/validation, generic object get/query/links, object sets, materialization run, Operations run list/detail, lineage get, transform retry, index replay, outbox DLQ retry, Record DLQ controls, observability, backup/restore, reconciliation, workflows, Iceberg maintenance read-only/POST planning, and Insight Review queue APIs. `GET /api/ontology/catalog` and `client.ontology.catalog()` expose the active object/property/action/link catalog for frontend workspace construction through the same `ontology:read` policy boundary. `GET /api/datasets`, `GET /api/datasets/{namespace}/{name}/inspect`, `GET /api/operations/lineage`, and `client.datasets.list/inspect(...)` plus `client.operations.lineage.get(...)` expose a Dataset Explorer backend start point, committed version/schema/manifest evidence, and lineage drill-down through the same public API/SDK surface lock. `docs/frontend-api-sdk-surface-matrix.json` classifies every FastAPI route as either frontend-consumable named SDK surface or non-frontend route, requires `proofClass: "sdk-request-contract"` for frontend rows, marks API routes that require `Idempotency-Key` with `requiresIdempotencyKey`, and also requires every `SDK_CLIENT_SURFACE.helpers` item to have an `sdkHelpers` row, TypeScript export, operator-evidence note, and helper-contract proof. `scripts/quality/check_frontend_backend_surface.py` fails if a route is unclassified, an SDK method/helper is missing, a FastAPI `Idempotency-Key` route is missing the matrix marker, the request/helper-contract proof test drifts away, or `apps/web/index.html` falls back to raw `/api/...` calls. `tests/sdk/request_contract.mjs` imports the actual browser SDK and fake-fetches all 42 frontend route surface rows, proving method/path/query/header/body contracts, context/request-id headers, idempotency headers, typed error metadata, browser/package object-query parity, missing-idempotency fail-fast behavior for every `requiresIdempotencyKey` SDK surface, and the runtime behavior of 12 SDK helpers including `retryWithBackoff(...)`, `collectCursorPages(...)`, `createInFlightActionLock()`, `actionLockKey(...)`, `requestContextHeaders(...)`, `createRequestId(...)`, `normalizeFoundryLiteError(...)`, and `classifyFoundryLiteError(...)`. `apps/web/index.html` now uses named SDK methods instead of `sdkClient().request(path, options)` for its current product controls, still passes tenant/user/role context through SDK context headers, and displays the latest request id, error code, and retryability in the Operations top bar. `quality:frontend-backend-surface`, `quality:sdk-request-contract`, and `quality:frontend-foundation` cover SDK/browser parity, generated output freshness, the API/SDK/docs surface matrix, browser request/helper contracts, and Web Operations static contract evidence. Full login/session UI, screen-specific retry/backoff UX, visual cursor pagination UX, duplicate-click button state UX, stale object-version compare/refresh UI, permission-denied masking UX, full catalog-driven S62-S64 workspace UX, and richer visual drill-down workflows remain future.
- S62 Dataset Explorer is partial for backend/API/SDK foundations, not the full visual explorer. The current public surface can list datasets, list versions, preview rows, inspect committed schema/manifest/file evidence, and fetch lineage through `GET /api/datasets`, `GET /api/datasets/{namespace}/{name}/versions`, `GET /api/datasets/{namespace}/{name}/preview`, `GET /api/datasets/{namespace}/{name}/inspect`, `GET /api/operations/lineage`, generated `client.datasets.*`, and generated `client.operations.lineage.get(...)`. `quality:frontend-backend-surface` and `tests/sdk/request_contract.mjs` keep those routes tied to named SDK methods and request-contract proof, while smoke coverage proves the API path returns committed dataset evidence. Full visual dataset browser, preview grid UX, version pinning UI, lineage graph navigation, and richer workspace interaction remain future.
- S63 Insight/Action Workspace now has a partial backend/API/SDK slice for durable review state, not the full visual workspace: `insight_reviews` stores tenant-scoped review rows with evidence refs, action proposal payloads, assignment state, decision state, and idempotency keys. `foundry.insights`, `GET/POST /api/insights/reviews`, `GET /api/insights/reviews/{review_id}`, `POST /api/insights/reviews/{review_id}/assign`, `POST /api/insights/reviews/{review_id}/decision`, and generated `client.insights.reviews.list/create/get/assign/decide(...)` expose the queue to the frontend without raw API path construction. `quality:insight-review` proves create idempotency, status/assignee filtering, one terminal decision winner, API create/assign/decision behavior, duplicate request replay, terminal decision conflict, and durable `insight_review.created`, `insight_review.assigned`, `insight_review.approved`, `insight_review.rejected` audit evidence. Evidence panel UI, model-diff UI, approval policy UI, action execution orchestration, and full Insight/Action Workspace screens remain future.
- PostgreSQL JSONB object store with production indexes and row-level security.
- Production connector registry/config persistence, managed retry workers, and more SaaS-specific connector adapters.
- Real CEL or JSON Logic evaluator.
- S53 External Writeback + Saga/Reconciliation is partial for unresolved external writeback safety and operator-provided remote-success reconciliation: `quality:external-writeback` proves simulated before-commit response loss becomes durable `outcome_unknown` action/writeback/audit evidence, while `quality:saga-reconciliation` proves simulated external success plus local mutation failure becomes durable `compensation_required` action/writeback/audit evidence, proves an operator-confirmed remote success can reconcile the original local mutation with one concurrent winner, and proves a sensitive writeback action parameter stays masked in Operations/audit surfaces. Same-idempotency-key replay returns the existing unresolved run instead of issuing another writeback. Real vendor writeback, vendor remote lookup, compensation worker execution, persistent operator queue, and approval UI remain future.
- S54 Data Quality Contracts is partial for versioned quality evidence: `dataset_check_results` now records `checked_manifest_hash`, `validated_against_schema_version_id`, and `validated_against_schema_version` for the staged candidate and schema row used by commit-time dataset quality checks. `quality:data-contracts` covers both the public dataset commit path and dataset quality repository contract, keeps historical run evidence pinned after later schema versions are created, stores successful check rows as `PASS`, keeps warning severity failures visible as non-blocking `WARN`, rejects a staged candidate changed between quality checks and storage commit, surfaces commit-time hard failures as `BLOCK_COMMIT`, and proves row-level `not_null`/`unique` quarantine routes failed records to Record DLQ while committing only the revalidated clean candidate. Operations run detail now exposes a transaction-scoped `quality` report with summary, schema references, checked manifest hashes, check results, and up to 5 data-quality quarantine failed-row samples. Full DataContract CRUD, owner notification, dedicated failed-row sample UI, quality history/trend, and production DB schema race proof remain future.
- Real ERP/webhook writeback connector, vendor remote lookup, compensation action worker, persistent reconciliation queue, and retry worker.
- Production Temporal worker deployment/operations beyond the adapter/time-skipping ratchet and S52 `ConnectorSyncWorkflow` control-plane slice.
- S55 DB/Dataset/Ontology Schema Migration is partial for migration safety, Dataset schema-evolution evidence, and Ontology migration plan evidence: `quality:schema-migrations` proves the Alembic chain is single-headed and forward-fix safe, requires `migration_phase` and `release_compatibility`, blocks old-writer-incompatible expand operations, rejects phase/window mismatch, and keeps contract cleanup fail-closed; `quality:schema-revision` plus migration parity still prove the current SQLAlchemy metadata is represented by revision evidence and a fresh Alembic upgrade; `quality:schema-migration-runner` proves the dedicated `db:migrate` runner uses a singleton DB lock instead of app/worker startup races and leaves operator evidence on failed migration attempts; `quality:schema-evolution` proves Dataset schema widening metadata plus idempotent backfill progress skeletons; and `quality:ontology-migrations` proves activation-time Ontology breaking-change blocking, deterministic object reindex plan evidence, and generated SDK compatibility checks. Full rolling deploy compatibility tests, actual ontology migration executors/reindex workers, actual backfill execution/progress APIs, live PostgreSQL contention proof, production migration runbooks, and deployment rollback tests remain future.
- Operations UI beyond the current object explorer/object-set controls, filtered Operations panel/API/CLI, request-id/error/retryability telemetry, run detail investigation view, failed transform retry, object-type index replay, failed index-run replay, DLQ materialization reprocess action, Record DLQ list/detail/replay/discard controls, and the named SDK-only frontend surface lock, especially failed sync/action/materialization-run replay, dynamic catalog-driven workspace APIs, and richer visual drill-down workflows.
- Sprint 02A Scale Foundation production completion: WorkflowAdapter, StreamAdapter, SearchIndexAdapter, and ConnectorAdapter ports are extracted with local/fake contract tests, StreamAdapter now has raw archive writer proof plus a production-compatible Kafka adapter/worker path with live Kafka-compatible broker smoke evidence, WorkflowAdapter has a Temporal adapter ratchet for idempotent start, timeout, unavailable, cancellation, and durable run error payloads, and SearchIndexAdapter now has Elasticsearch-compatible projection/rebuild/live-cluster proof. Sprint 39 now has standard CDC envelope archive proof for Debezium-shaped events plus live Debezium Connect/PostgreSQL topic proof, Sprint 40 has CDC object indexing proof for object base patch upsert, tombstone delete, idempotent replay, stale-event skip, and object.changed trigger emission, Sprint 42 has search projection proof, S52 has a partial `ConnectorSyncWorkflow` product workflow control-plane proof through local/fake/Temporal workflow profiles, and S58A has local JWT/OIDC bearer-token verification through the `AuthProvider` boundary plus REST connector secretRef re-resolution through `SecretProvider`. Production connector adapters, continuously running CDC/search workers, deployment-specific Redpanda/Elasticsearch packaging, managed Temporal worker operations, live OIDC discovery/JWKS polling, service-account/session lifecycle, full workflow connector credential refresh, and cloud/Vault secret managers remain future work. The local repositories still rely on SQLAlchemy under the hood.
- Postgres testcontainer contract pairing now covers every current SQLAlchemy repository contract suite (Sprint 9.4): `metadata`, `dataset_repository`, `dataset_version`, `dataset_transaction`, `dataset_quality`, `transform`, `ontology`, `object_read`, `object_index`, `object_set`, `action`, `materialization`, and `runtime`. The local escape hatch `FOUNDRY_LITE_SKIP_POSTGRES_CONTRACTS=1` exists only for Docker-unavailable developer machines; `pnpm ci:gate` rejects it so release/CI evidence cannot skip PostgreSQL parity. The gate now runs `scripts/quality/check_testcontainers_preflight.py` before pytest so a Docker/Colima socket problem fails early with `DOCKER_HOST` and `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE` guidance instead of a long Testcontainers traceback. The same preflight also protects the Kafka live broker smoke because it uses Testcontainers Docker access.
- The closed-loop demo is repeatable as a fresh one-command smoke, DLQ materialization reprocess is available, failed transform retry is available, and object-type plus failed-run-specific index replay are available across API, CLI, and web. Replay operations such as retrying failed sync/action/materialization runs and action-edit reindex replay remain future Operations UI/CLI work.

## Refactor Pointers

These are known design debts that are currently gated (so they cannot regress) but
should be paid down on a planned timeline. They are not bugs; they are deliberate
intermediate states.

- **Application Service canonicalization.**
  `FoundryLite` no longer composes service capabilities through Python
  multiple inheritance. It is a platform root that exposes one thin facade per
  bounded context (`DatasetWorkspace`, `TransformPipeline`, `OntologyRegistry`,
  `ObjectStore`, `ActionGateway`, `MaterializationRunner`, `OperationsConsole`,
  `SupplyChainDemo`), each holding explicit public forwarders,
  while `CoreServices` constructs `ActionService`, Dataset service group,
  Object service group, Transform, Ontology, Materialization, Runtime, and Demo
  services with service-specific dependency injection. A service receives only
  the `CoreDependencies` fields listed in its `required_dependencies`; for
  example, `ActionService` receives `engine`, `policy`, and `action_repository`,
  not storage or transform adapters. A service also receives only the
  collaborator services listed in its `required_collaborators`; for example,
  `ActionService` receives runtime, ontology, object-record, and object-indexing
  collaborators, not all 16 services. Cross-service helper access now uses
  explicit collaborator attributes such as `runtime_service._audit(...)` rather
  than `__getattr__` method lookup. These rules are held in check by static gates:
  `check_service_dependencies.py`, `check_service_call_graph.py`, Tach, AST-grep,
  and the application-size cap. This keeps the
  public API stable while removing facade-level MRO risk before Workflow /
  Stream / Search / Connector / Auth boundaries are extracted.
- **Remaining Facade compatibility debt.**
  The flat service method registry and `FoundryLite.__getattr__` /
  `FoundryLite.__setattr__` monkeypatch bridge have been removed. The
  remaining compatibility cost is the explicit public forwarder list spread
  across the bounded-context sub-facades, which keeps API/CLI/test entrypoints
  stable while services continue to evolve independently.

## Quality Signal Boundaries

- Branch coverage is the main behavior gate.
- Public callable coverage means "a public callable was executed at least once"; it is not a substitute for branch/path coverage.
- CI enforces `0` `core._...` private facade references in tests. Some lower-level tests still target service/helper modules directly to pin failure edges, but the public facade no longer exposes or forwards private helper methods.
