# AGENTS.md instructions for Foundry-lite

## 필수 시작 절차

이 저장소에서 코드, 테스트, 문서, CI, 아키텍처 변경을 시작하기 전에 반드시 먼저 아래 문서를 읽는다.

1. `foundry_lite_python_engineering_guidelines_ko.md`
2. 변경 범위가 제품/스프린트 목표와 연결되면 `foundry_lite_development_plan_ko_sprintified.md`
3. 변경 범위가 sprint acceptance나 MVP core 완료 조건과 연결되면 `foundry_lite_sprint_breakdown_ko.md`
4. 현재 구현 상태와 문서상 목표를 구분해야 하면 `docs/implementation-status.md`
5. 새 정적/동적 분석 도구를 추가하거나 기존 게이트를 약화/삭제하려 하면 `docs/quality-gate-roadmap.md`

특히 `foundry_lite_python_engineering_guidelines_ko.md`는 이 repo의 Python 백엔드 구현 표준 원본이다. 개발 시작 전에 읽지 않은 상태로 구현을 진행하면 안 된다.

## 개발 원칙

- 비개발자도 이해할 수 있도록 항상 친절하고 자세히 설명한다.
- 문서와 코드가 다르면 코드를 과장하지 말고 현재 구현 상태를 정직하게 문서화한다.
- `FoundryLite`는 얇은 Facade로 유지한다.
- Dataset, Transform, Ontology, Object, Action, Materialization, Runtime event, Demo orchestration 책임을 다시 한 파일로 합치지 않는다.
- application module은 500줄 이하를 유지한다. 필요하면 책임별 service, strategy, specification, adapter, repository로 분리한다.
- 디자인 패턴은 `foundry_lite_python_engineering_guidelines_ko.md`의 “디자인 패턴 적용 원칙”을 따른다.
- infra와 닿는 변경은 Sprint 02A Scale Foundation 원칙을 먼저 확인한다. storage, metadata DB, compute, event, search, workflow, connector, auth 변경은 port/interface, adapter, contract test, trace key를 함께 고려한다.
- 새 mutation은 transaction, audit, outbox, idempotency, error traceability를 함께 고려한다.
- 새 infra dependency는 항상 `CoreDependencies` (`libs/foundry_lite/application/dependencies.py`)에 먼저 선언하고, 실제로 그 dependency를 직접 쓰는 service의 `required_dependencies`에만 추가한다. service가 선언하지 않은 CoreDependencies 필드에 접근하면 안 된다 — `scripts/quality/check_service_dependencies.py`가 이를 강제한다.
- service 사이의 collaborator call은 `self.runtime_service._audit(...)`처럼 소유 service가 보이는 명시적 attribute를 통해 호출한다. 각 service는 실제로 직접 호출하는 collaborator만 `required_collaborators`에 선언한다. `self._audit(...)`처럼 글로벌 method registry에 기대는 숨은 호출을 만들지 않는다. collaborator 그래프는 DAG로 유지한다. cycle 0, depth ≤ 7, service당 fan-out ≤ 10 — `scripts/quality/check_service_call_graph.py`와 `check_service_dependencies.py`가 강제한다.
- 변경 후에는 최소한 관련 테스트와 정적 검사를 실행한다. 로컬 기본 확인은 `pnpm ci:gate`로 빠른 static+impact gate를 돌리고, 전체 release evidence를 로컬에서 직렬 확인해야 할 때는 `pnpm ci:gate:all` 또는 `pnpm ci:gate:release`를 사용한다.
- 디자인 패턴/안티패턴 위반은 Semgrep rule (`scripts/quality/semgrep-rules/foundry-lite.yml`)로 코드 모양 자체를 차단한다. 위반을 우회하려고 `# nosemgrep:` 주석을 다는 경우 같은 줄에 정직한 사유와 미래 제거 조건을 적는다.
- Layer/import 경계는 `.importlinter` 4 contracts가 transitive 그래프로 강제한다. function-local lazy import도 잡힌다. contract 변경/약화는 `docs/quality-gate-roadmap.md` §5 워크플로를 따른다.
- Module dependency DAG는 `tach.toml`과 `uv run tach check --dependencies`가 강제한다. 새 app/library/boundary를 추가하면 먼저 어느 layer에 속하는지 `tach.toml`에 명시하고, circular dependency 0을 유지한다.
- Function length G3 게이트(`scripts/quality/check_function_length.py`)는 `libs/foundry_lite/application` 함수가 40줄을 넘으면 release gate를 차단한다. 현재 baseline은 0개이며, 새 40줄 초과 함수는 먼저 책임 분리나 helper/payload 분리를 검토해야 한다.
- Boolean naming C1 게이트(`scripts/quality/check_boolean_naming.py`)는 `libs`, `apps`, `scripts`의 boolean 인자와 annotated field가 `is_`/`has_`/`can_`/`should_`/`include_`/`allow_` 같은 질문형 또는 명시적 상태명으로 읽히지 않으면 release gate를 차단한다. 현재는 함수 반환 bool 이름까지 완전 판정하지 않는 부분 강제 게이트다.
- Dict-any G4 게이트(`scripts/quality/check_dict_any_budget.py`)는 application 함수 시그니처의 `dict[str, Any]` 총량과 layer별 총량이 현재 baseline보다 늘어나면 release gate를 차단한다. 남은 `dict[str, Any]`는 model/TypedDict/dataclass로 줄여야 할 schema drift 부채로 추적한다.
- Router layer purity G1 게이트(`scripts/quality/check_router_layer_purity.py`)는 `apps/api`가 `core.*_repository`나 DB transaction/execute를 직접 호출하면 release gate를 차단한다.
- Query side-effect 게이트(`scripts/quality/check_query_side_effects.py`)는 `get_*`, `list_*`, `query_*`, `preview_*`, `inspect_*`, `lineage_*`, `find_*` public service method가 repository write, audit/outbox/lineage write, 파일 write, write adapter, mutation collaborator로 이어지면 release gate를 차단한다.
- Repository no-business G10 게이트(`scripts/quality/check_repository_no_business.py`)는 `libs/foundry_lite/infrastructure/repositories`가 도메인 에러를 import/raise하거나 savepoint가 아닌 transaction을 직접 commit/rollback하면 release gate를 차단한다.
- Tenant write guard S1 게이트(`scripts/quality/check_tenant_write_guard.py`)는 tenant-scoped SQLAlchemy insert/update/delete가 `tenant_id` values 또는 `tenant_id` where guard 없이 실행되는 것을 release gate에서 차단한다.
- Contract-test-per-port G13 게이트(`scripts/quality/check_contract_test_per_port.py`)는 `libs/foundry_lite/application/ports`의 모든 port 파일이 `tests/contracts/test_*_contract.py` 대응 suite를 갖도록 강제한다.
- Strategy/Specification testability D1 게이트(`scripts/quality/check_strategy_specification_tests.py`)는 filter evaluator, precondition evaluator, `*Strategy`, `*Specification` 규칙 모듈이 직접 unit/property test import 없이 추가되면 release gate를 차단한다.
- Integration scenario G15 게이트(`scripts/quality/check_integration_scenario_markers.py`)는 MVP release 필수 통합 시나리오 7개가 `@pytest.mark.integration_scenario(...)`로 모두 표시되어 있지 않으면 release gate를 차단한다.
- Root-cause patch prevention G21/G22 게이트(`scripts/quality/check_regression_test_per_bugfix.py`, `scripts/quality/check_pr_root_cause_section.py`)는 fix/bug/patch/regression 커밋이 같은 커밋의 `tests/` 변경 없이 들어오거나, PR 본문에 Root Cause, Impact, Regression Test 섹션이 없으면 release gate를 차단한다.
- Document drift G14 게이트(`scripts/quality/check_doc_drift.py`)는 repo Markdown 문서와 `docs/*.json` 문서가 언급한 source path, script, package script, FastAPI route, pytest node id, Markdown local link target/heading anchor가 실제 repo에 없으면 release gate를 차단한다. Python class/method 심볼 검사는 과장 위험이 큰 current-state boundary 문서인 `AGENTS.md`와 `docs/implementation-status.md`에 더 엄격하게 적용한다. 미래 목표나 미구현 gap이라고 정직하게 적은 문장은 코드 참조 검사 대상에서 제외하지만, 현재 명령처럼 적힌 `quality:*`/`ci:*`/`pnpm --silent ...`는 `package.json` scripts와 맞아야 하고 문서 링크는 클릭 가능해야 한다.
- Evidence ledger command 게이트(`scripts/quality/check_evidence_ledger_commands.py`)는 `docs/sprint-evidence-ledger.md`의 proof command가 실제 `package.json` script, Python/Node/bash script file, test path, pytest node id를 가리키지 않으면 release gate를 차단한다.
- Documentation map G14B 게이트(`scripts/quality/check_documentation_map.py`)는 `docs/documentation-map.md`의 Document Roles, doc-drift scan inventory, Source Of Truth Rules, Update Order, README 문서 지도, Cross-Check Commands, 그리고 AGENTS의 active documentation/proof gate 설명이 현재 repo와 어긋나면 release gate를 차단한다.
- Proof/source/operator evidence 계약(`scripts/quality/check_proof_matrix.py`, `scripts/quality/check_source_of_truth_contract.py`, `scripts/quality/check_operator_evidence_contract.py`)은 tricky infra matrix의 proof class, serving source-of-truth invariant, durable operator evidence가 문서에만 남지 않고 실제 pytest/CI 증거와 연결되는지 확인한다. 실패 원인은 로그 한 줄이 아니라 run/audit/transaction/error payload처럼 나중에 운영자가 다시 볼 수 있는 증거에 남아야 한다.
- Semantic doc consistency S46 게이트(`scripts/quality/check_semantic_doc_consistency.py`)는 active-covered infra를 범위 설명 없이 future로 되돌려 쓰거나, README가 deferred/partial data pattern을 current처럼 쓰면 release gate를 차단한다.
- Data pattern matrix S46 게이트(`scripts/quality/check_data_pattern_matrix.py`)는 `docs/data-engineering-pattern-matrix.json`의 pattern status, proof level, owner, reason, future test, README alias, active infra reference가 빠지거나 틀리면 release gate를 차단한다.
- Data platform sprint status 게이트(`scripts/quality/check_data_platform_sprint_status.py`)는 S46-S64 status table과 high-level current/future boundary가 `docs/data-platform-expansion-sprint-plan-ko.md`, `foundry_lite_sprint_breakdown_ko.md`, `README.md`, `docs/implementation-status.md` 사이에서 달라지면 release gate를 차단한다.
- Schema revision guard 게이트(`scripts/quality/check_schema_revision_guard.py`)는 `libs/foundry_lite/infrastructure/schema` 패키지의 SQLAlchemy metadata fingerprint가 `infra/schema_revisions/*.json` 최신 snapshot과 다르면 release gate를 차단한다. S55 schema migration safety 게이트(`scripts/quality/check_schema_migrations.py`)는 Alembic migration history가 단일 head이고 revision id가 filename과 맞으며 `downgrade()`가 destructive schema operation을 수행하지 않는 forward-fix-only 정책인지 검증한다. 또한 migration마다 `migration_phase`와 `release_compatibility`를 요구하고, expand 단계에서 drop/alter/rename류 operation, 검토 불가능한 SQL, 기본값 없는 새 `NOT NULL` 컬럼, phase/window mismatch, 준비 안 된 contract cleanup을 차단한다. S55 migration runner(`scripts/operations/run_migrations.py`, `db:migrate`, `quality:schema-migration-runner`, `quality:schema-migration-runner-live`)는 Alembic을 전용 runner에서만 실행하고 PostgreSQL advisory lock 또는 SQLite `BEGIN IMMEDIATE` DB lock으로 concurrent migration job 중 하나만 실행되도록 검증하고, live PostgreSQL contention에서도 한 runner만 migration callback을 실행하는지 확인하며, 성공/lock-busy/실패 결과를 password-masked operator evidence JSON으로 남긴다. S55 Dataset schema evolution gate(`libs/foundry_lite/application/services/dataset/schema_evolution.py`, `quality:schema-evolution`)는 Dataset commit 전 rename/drop/type narrowing/primary-key change를 blocking change로 남기고, numeric widening/deprecated field/backfill-needed change를 transaction metadata로 남긴다. S55 Ontology migration gate(`libs/foundry_lite/application/services/ontology_migration.py`, `quality:ontology-migrations`)는 active ontology와 candidate YAML을 비교해 property/object/link/action parameter의 consumer-breaking 변경을 activation 전에 차단하고, deprecated property warning, previous/next source-column mapping evidence, object reindex plan, generated SDK compatibility evidence, bounded object reindex executor evidence를 audit/outbox와 gate에 남긴다. S56 observability detector gate(`libs/foundry_lite/application/services/observability_detectors.py`, `quality:observability-detectors`, `quality:slo-contracts`)는 versioned detector config를 현재 runtime snapshot에 read-only로 적용해 missing-data, lag, skew, cooldown dedupe, SLO evidence를 검증하고, `observability_incidents` 저장형 lifecycle(open/acknowledged/resolved), audit/outbox, API/SDK surface가 끊기지 않는지 검증한다. S57 backup/restore preflight gate(`libs/foundry_lite/application/services/backup_restore_service.py`, `quality:backup-restore`)는 metadata DB의 committed dataset version과 storage manifest/data file byte/hash가 일치하는지 검증하고, missing/corrupt version을 `blocked` report로 남기며, ready preflight를 immutable local backup artifact receipt로 저장하고, artifact restore entrypoint에서 receipt hash와 current serving head drift를 검증하고, execute path에서 byte/hash-clean artifact source files를 새 snapshot dataset version으로 복원해 latest serving head로 커밋하며, active object index pointer, action/outbox/audit high-watermark, Temporal strategy, search rebuild marker, post-restore validation evidence, API/SDK surface가 끊기지 않는지 검증한다. full old/new app compatibility window, object/link remap executor, real backfill execution/progress API, multi-step upgrade/rollback 운영 runbook, external DB dump orchestration, platform-wide edge traffic gate, outbox publisher pause/resume executor, automatic restore smoke execution, notification delivery, dashboard/timeline UI, broker-offset/REST-cursor lag adapter, persistent threshold registry는 아직 future scope다. DB 모양을 바꾸는 변경은 schema revision snapshot, Alembic migration, migration safety evidence를 같은 변경에 포함해야 한다.
- S57 restore-mode lockout slice(`BackupRestoreService.start_restore_mode`, `restore_mode_status`, `RuntimeService.dead_letter_event_retry_plan`)는 restore mode 상태를 audit evidence로 남기고, 같은 `restoreId` 재시도는 중복 restore-mode audit event를 만들지 않으며, restore mode 중 outbox dead-letter retry와 materialization reprocess 진입을 차단한다. 이는 real publisher daemon pause/resume executor나 platform-wide write traffic gate가 아니라, 현재 outbox retry 경로의 외부 side effect 중복 방지 증거다.
- S57 restore resume approval slice(`BackupRestoreService.approve_restore_resume`)는 post-restore dataset inventory, active object index pointer, action run, materialization run 증거가 없으면 publisher resume 승인을 거절하고, 검증 통과 후 `resume_approved` audit evidence로 현재 outbox retry/reprocess entrypoint 잠금을 해제한다. 실제 automatic smoke 실행, real publisher daemon pause/resume, full production restore rehearsal은 아직 future scope다.
- S58A auth/secret-provider slice(`JwtOidcAuthProvider`, `SecretProvider`, `SecretValue`, `EnvSecretProvider`, `RestPullConnectorAdapter`, `quality:auth-secrets`)는 local discovery/JWKS 기반 JWT/OIDC human/M2M bearer-token 검증, local revoked-JWT denylist, 웹훅 서명키/local secret lookup, REST connector bearer/header secretRef 재조회를 application port로 통과시키고, secret value가 repr/operator evidence/error detail에 노출되지 않는지 검증한다. 이는 live OIDC discovery fetch, JWKS polling/TTL/key retirement, IdP introspection/refresh-token revocation, service-account registry/scope policy, cloud/Vault secret manager, full connector workflow credential refresh, full rotation lifecycle이 아니라 현재 local JWT/OIDC verification + local-env/redaction + REST connector secretRef refresh 증거다.
- S58B privacy transform slice(`PrivacyTransformPlan`, `PrivacyFieldRule`, `PrivacyReplicationPolicy`, `PrivacyDatasetRef`, `InMemoryProtectedPrivacyMappingStore`, `transform_privacy_rows`, `build_privacy_openlineage_event`, `quality:privacy`)는 tenant-scoped deterministic pseudonymization, basic field anonymization, local regex PII redaction, replay-stable plan version/lineage metadata, protected in-memory reversible mapping proof, production-to-nonprod replication policy proof, source-to-anonymized target dataset version lineage, raw-value-free OpenLineage-compatible event artifact를 검증한다. 이는 durable environment replication workflow, durable/encrypted protected mapping backend, ontology-driven privacy registry, runtime DB `lineage_edges`/outbox/OpenLineage transport integration, right-to-erasure lifecycle이 아니라 현재 privacy transform + protected mapping + replication policy + lineage artifact proof 증거다.
- S58C erasure manifest slice(`ErasureRequest`, `ErasureRetentionPolicy`, `resolve_erasure_subject`, `ErasureManifest`, `is_erased_resource`, `quality:erasure`)는 deletion request redacted evidence, tenant-scoped subject resolution, stable/idempotent deletion manifest, backup-retention pending state, audit-minimization action, search rebuild exclusion proof를 검증한다. 이는 durable request table/API/workflow, 실제 ObjectStore/SearchAdapter/materialization/DLQ/backup/audit executor가 아니라 현재 right-to-erasure manifest planning proof 증거다.
- S60 AI evidence slice(`propertyLineage`, `EvidenceReference`, `EvidenceSourceSpan`, `build_insight_claim_payload`, `build_llm_extraction_evidence`, `revise_evidence_reference`, `quality:ai-evidence`)는 object explain property-level source 좌표, insight claim evidence-object requirement, LLM extraction model/prompt/extractor version pinning, immutable evidence revision, masked source-span redaction을 검증한다. 이는 durable evidence table, 실제 LLM executor, insight evidence viewer, model diff UI, AI action policy enforcement가 아니라 현재 fine-grained lineage/evidence reference proof 증거다.
- S61 frontend foundation slice(`createRequestId`, `requestContextHeaders`, `normalizeFoundryLiteError`, `isRetryableFoundryLiteError`, `retryWithBackoff`, `collectCursorPages`, `createInFlightActionLock`, `createFoundryLiteOsdkClient`, `createFoundryLiteOntologyIndex`, `sdkPackageManifest`, `sdkOntologyDriftReport`, `assertFoundryLiteSdkFresh`, `createSessionTokenProvider`, `pollFoundryLiteOperation`, `streamFoundryLiteOperationEvents`, `getObjectType`, `getActionType`, `adminCapabilityView`, `adminOperationsBoard`, `adminReadinessScreen`, `adminTaskView`, `adminTaskPlanScreen`, `groupAdminCapabilities`, `quality:frontend-foundation`)는 generated SDK request wrapper, session token provider, tenant/user/role context header, request id propagation/display, typed frontend error, retryability 표시, 262개 frontend route surface, 28개 SDK helper surface, Ontology-style object/action/media OSDK facade, generated package manifest/fingerprint exposure, live-catalog SDK regeneration assertion, large ontology registry lookup/live-catalog search/action grouping/dynamic-only drift hint, Compass-style Projects/Resources DB/API/SDK surface, Pipeline Builder graph API/SDK surface, React live ontology catalog workspace model, bounded long-running operation polling, fetch-based operation event streaming helper, Source onboarding recipe/hook, Source Wizard recipe/hook, Generic REST connector onboarding recipe/hook, Source scheduler schedule edit/pause/resume, 연속 실패 자동 일시정지, 복구 빌드 성공 후 자동 재개, preview/tick API/SDK와 실패 진단 UI, React cursor pagination screen-state helper, React start-and-poll long-running job helper, React operation event stream state helper, React admin console launch/preflight/task-board model, admin readiness overview/screen model, admin task plan screen model, admin capability `executionSurface`/`canStartFromBrowser`/`requiresApproval`/`operatorChecklist`/`blockingReason` safety fields, bounded outbox admin publish 시작점, typechecked `@foundry-lite/sdk/screen-recipes` core product-screen builders, screen-level SDK recipe contract, 문서 count-claim guard, Web Operations SDK request routing을 검증한다. 이는 full login/session UI, screen-specific retry/backoff copy, visual cursor component UX, broader visual server-push timeline components, visual streaming timeline UX, duplicate-click button state UX, stale object version conflict UI, permission-denied masking UX, folder/resource direct grant override, NetSuite/OAuth packaged source wizard, SharePoint Graph 실행형 adapter, direct migration execution, generic long-running worker daemon control from the browser, infra bootstrap browser execution이 아니라 현재 frontend SDK foundation proof 증거다.
- Data Connection 관리형 Source lifecycle slice(`SourceLifecycleService`, `PATCH /api/sources/{source_name}/status`, `client.sources.updateStatus`)는 Source와 Sync를 별도 durable resource로 유지하고, config fingerprint CAS와 `Idempotency-Key`로 Source disable/re-enable을 수행한다. disabled Source는 기존 Sync/run/commit 이력을 보존하되 새 manual/recovery run과 scheduler start를 차단하고 `source.disabled` audit/outbox를 남기며, re-enable은 `source.enabled` 증거와 함께 실행 제어를 복구한다. 이는 외부 DB 계정 폐기나 원격 connector daemon 종료가 아니라 현재 Foundry-lite Source 실행 경계의 lifecycle proof다.
- Data Connection Source live diagnostic slice(`SourceConnectionTestService`, `POST/GET /api/sources/{source_name}/connection-tests`, `GET /api/sources/{source_name}/egress-attempts`, `client.sources.testConnection/listConnectionTests/listEgressAttempts`)는 현재 Source config fingerprint와 `Idempotency-Key`를 요구하고, REST는 실제 resource preview, database는 실제 `SELECT 1`과 visible resource count를 실행하되 Dataset commit을 만들지 않는다. Agent mode는 fresh daemon heartbeat, `proxyUrl`, network policy host/port allowlist를 확인한 뒤 REST의 HTTP/TLS 요청과 PostgreSQL driver의 raw TCP bytes를 Agent HTTP CONNECT 터널로 전달하며 실패 시 direct로 우회하지 않는다. PostgreSQL은 ephemeral loopback bridge와 libpq `hostaddr`를 사용해 driver는 로컬 bridge로 연결하되 원래 database hostname은 인증/TLS 의미를 위해 보존한다. endpoint/network/worker/credential/preview 단계, redacted error, request ID, audit/outbox, connectionId/responseFlags/bytes/duration/port/origin/policy/agent egress 증거를 `source_connection_tests`에 저장하고 Data Connection 연결 설정 화면에서 새로고침 뒤에도 진단·egress 이력을 다시 보여준다. 관리형 REST와 PostgreSQL Source sync는 실행 시점의 최신 Source -> policy -> fresh Agent route를 다시 해석해 같은 CONNECT transport로 snapshot을 커밋하며, Dataset transaction metadata의 `connectorNetworkEvidence` 또는 `sourceNetworkEvidence`에 실제 connection ID, response flags, bytes, duration, destination port, origin, policy, Agent를 영구 저장한다. Source run API/SDK는 이 transaction metadata provenance를 `networkEvidence`로 올리고, Data Connection 실행 이력의 `실제 빌드 네트워크 경로` 카드가 설정값이 아닌 실제 Agent/direct 경로, policy, Agent, destination, bytes, duration, response flags, connection ID를 표시한다. 이는 outbound-WebSocket production Agent control channel, Source terminal, central multi-node egress aggregation, broader database-engine packaging, vendor-specific health check가 아니라 현재 local Agent CONNECT runtime + REST/PostgreSQL live probe/preview/sync proof다.
- PostgreSQL Data Connection browser slice(`apps/foundry/src/features/data-connection/wizard/ManagedSourceFlow.tsx`, `apps/foundry/src/features/data-connection/wizard/ManagedSourceSteps.tsx`, `apps/foundry/src/features/data-connection/detail/SyncNetworkEvidenceCard.tsx`, `apps/foundry/src/features/data-connection/SyncsPanel.tsx`)는 `postgres_jdbc` 선택 시 REST용 Bearer/API-key 옵션이 아니라 `database_url`을 기본값으로 사용하고 PostgreSQL connection URL 전용 vault 입력 안내를 보여준다. Source 상세와 전역 동기화 상세는 같은 네트워크 증거 카드를 재사용해 Agent route evidence를 누락 없이 표시한다. Database URL 자체는 redacted secret으로 유지하면서 네트워크 설정은 연결된 policy의 실제 allowlist를 표시한다. 연결 진단은 5단계와 egress history를 남기고 Source Explorer는 remote table/column/sample row를 Dataset commit 없이 보여준다. `tests/e2e-foundry/data-connection-managed-source-flow.spec.ts`가 PostgreSQL credential UI와 두 실행 상세 surface를 회귀 검증한다.
- SAP OData Data Connection slice(`libs/foundry_lite/infrastructure/adapters/rest_connector.py`, `libs/foundry_lite/application/services/source_management_run_helpers.py`, `apps/foundry/src/features/data-connection/wizard/ManagedSourceFlow.tsx`, `apps/foundry/src/features/data-connection/explore/RestSourceExplorer.tsx`)는 Generic REST transport를 SAP OData 읽기 전용 패키지로 특화한다. Basic `username:password`는 secretRef로만 adapter에 전달하고, 같은 origin의 `@odata.nextLink`만 따라가며, 한 snapshot에서 최대 100 page를 bounded 수집한다. 등록된 SAP Source의 연결 진단·Source Explorer·managed sync는 모두 Source network policy/Agent route를 다시 해석하고, 미리보기는 두 page/sample/network evidence를 보여주되 Dataset commit을 만들지 않으며, sync는 전체 page를 한 Dataset version으로 commit한다. Source 상세 개요는 Batch sync, Source exploration, Connection diagnostics capability를 실행 가능 여부와 함께 표시하고, 자격 증명 설정은 위저드가 만든 Basic secretRef를 다시 읽고 교체할 수 있다. Marketplace도 `executionStatus`를 그대로 사용해 SharePoint Graph 같은 `definition_only` 항목의 실행 딥링크를 비활성화한다. `tests/contracts/test_rest_connector_adapter_contract.py`, `tests/integration/test_connector_onboarding.py`, `tests/e2e-foundry/data-connection-managed-source-flow.spec.ts`, `tests/e2e-foundry/marketplace-source-template-flow.spec.ts`가 Basic redaction/lifecycle, cross-origin next-link 차단, multi-page snapshot, capability action, catalog 실행 상태, OData 기본값을 회귀 검증한다. SharePoint Graph는 현재 `definition_only`, NetSuite와 SAP RFC/BAPI/writeback은 future다.
- Facade magic fallback은 AST-grep rule (`scripts/quality/ast-grep-rules/no-facade-magic-dispatch.yml`)이 차단한다. `FoundryLite`에 `__getattr__`/`__setattr__` method-registry dispatch를 다시 넣지 않는다.
- Secret/credential 노출은 `.gitleaks.toml`로 차단한다. 로컬에서는 gitleaks 미설치 시 경고만 가능하지만, CI/release evidence에서는 미설치가 곧 실패다. 새 false positive를 발견하면 allowlist에 사유와 함께 등록한다. 시크릿이 실제로 들어왔다면 즉시 revoke + git history 정리한다.
- Cross-function 데이터 흐름 (request → SQL, request body → service, mutation → audit, exception → request_id)은 CodeQL queries (`scripts/quality/codeql/queries/`)로 검사한다. CodeQL DB build는 Python 코드베이스 기준 fresh build 3~5분 + analyze 1~2분이므로 로컬 `pnpm ci:gate`에서는 돌리지 않는다. CI(`.github/workflows/codeql.yml`)가 push/PR마다 강제 실행하고, SARIF finding은 `scripts/quality/codeql/fail_on_sarif_findings.py`가 hard failure로 바꾼다. 새 violation 패턴을 발견하면 동일 디렉터리에 `.ql` 쿼리를 추가하고 `tests/unit/test_quality_codeql_queries.py`에 § 인용·@id·@kind 메타와 알려진 CodeQL API 호환성 검증을 추가한다.
- Audit-on-mutation G2 게이트(`scripts/quality/check_audit_on_mutation.py`)는 public application service mutation entrypoint가 repository write에 닿으면서 audit/outbox 경로가 없으면 release gate를 차단한다.
- Transaction outbox/audit pair G11 게이트(`scripts/quality/check_transaction_outbox_pair.py`)는 service transaction block 안에서 repository write가 일어날 때 같은 transaction call tree 안에 audit/outbox 증거가 없으면 release gate를 차단한다.
- Action idempotency G12 게이트(`scripts/quality/check_idempotency_on_action.py`)는 Action API의 `Idempotency-Key` header, Core/Service 전달, 기존 action_run 재사용 조회, action_runs unique key가 끊기면 release gate를 차단한다.
- Error response request-id G7 게이트(`scripts/quality/check_error_response_has_request_id.py`)는 FastAPI error detail 또는 `_handle_error` 호출에서 `request_id`가 빠지면 release gate를 차단한다.
- Log trace-key G5 게이트(`scripts/quality/check_log_has_trace_keys.py`)는 직접 logger 호출이나 `log_event` 호출이 `request_id`, `tenant_id`, run id 계열 추적 키 없이 남는 것을 release gate에서 차단한다.
- Metrics exposure 게이트(`scripts/quality/check_metrics_exposed.py`)는 가이드 §12.2의 dataset commit, transform run, action apply, object query, outbox lag, failed run, DLQ size 지표 7개가 Prometheus payload에 없으면 release gate를 차단한다.
- Runtime audit-count G17 게이트(`scripts/quality/check_audit_count_runtime.py`)는 supply-chain demo smoke 후 runtime DB의 high-level mutation 증거와 durable `audit_events` row 수가 정확히 일치하는지 검증한다.
- Outbox consistency G18 게이트(`scripts/quality/check_outbox_consistency.py`)는 supply-chain demo smoke 후 event-propagated state change와 durable `outbox_events` row가 event type, aggregate, idempotency key, correlation key 기준으로 1:1 또는 run-count 기준에 맞는지 검증한다.
- Record DLQ replay runtime 게이트(`quality:record-dlq-replay`)는 stream CDC archive에서 불량 CDC envelope record가 `dead_letter_records`로 격리되고 정상 record commit은 계속되며, DLQ 저장 실패가 main pipeline 성공으로 숨지 않는지 검증한다. 또한 Operations API와 generated TypeScript SDK의 record DLQ 목록/상세, 단건·bulk replay idempotency, 실제 replay commit/result, replay failure result, discard, replay/discard audit evidence, Web Operations record DLQ UI control evidence, source-side error threshold fail-closed, identity/ordering fail-closed, PostgreSQL concurrent replay proof, Python transform row-error quarantine proof, snapshot transform-specific DLQ full-rebuild retry proof를 확인한다. 현재는 S47 partial slice이며 append/incremental transform DLQ replay는 duplicate-safe merge policy 전까지 future work다.
- Late-data/watermark runtime 게이트(`quality:late-data`, `quality:watermark`)는 stream/source archive에서 `event_time`, `source_time`, `ingested_at`, `processed_at`가 분리되고, source별 event-time/source-time field, named timezone override, lateness threshold가 `StreamArchiveConfig`로 선언되며, too-late event가 `TOO_LATE` Record DLQ로 격리되고, `dataset_transactions.metadata.lateDataWatermark`가 matching partition/source별로 뒤로 이동하지 않는지와 느린 partition이 다른 partition watermark로 오분류되지 않는지 검증한다. 현재는 S48 stream/source + snapshot transform output + materialization boundary propagation partial slice이며 materialization action/object top-level `platformWatermark`, materialization reopen/reprocessing plan, normalized `platformWatermark`, transform output min-input event-time watermark, late-data badge, downstream impact, stale late-delete guard까지 active proof가 있다. full platform-wide watermark semantics는 future work다.
- Multi-file dataset runtime 게이트(`quality:multi-file-dataset`, `quality:partition-pruning`, 그리고 profile별 `quality:s3-storage`/`quality:iceberg`)는 `DatasetStorageAdapter.data_file_paths()`가 manifest-listed data files를 순서대로 resolve하고, public `foundry.datasets.preview(...)`가 첫 파일만 읽거나 directory/bucket listing으로 manifest 밖 file을 serving data에 섞지 않는지, local/fake/S3 read/preview path에서 `partition_filter`가 실제 read file 수를 줄이는지, readable Parquet commit path가 manifest `column_stats`/`sort_bounds`를 남기는지, local/fake/S3 storage에서 여러 staged part가 하나의 dataset version으로 commit되고 local/fake application transaction에서는 DB `dataset_files` row와 preview가 같은 file set을 보는지, high-cardinality partition warning이 committed transaction metadata에 남는지, tampered part와 stale retry orphan part가 막히는지, S3 same-version retry가 part 중복 없이 성공하는지, Iceberg가 여러 staged part를 하나의 pinned snapshot 안의 여러 data file로 커밋하고 retry/duplicate conflict를 구분하는지, Iceberg no-match filter가 snapshot read를 건너뛰는지, Iceberg column-stat filter가 전체 snapshot materialization 없이 matching data file만 다운로드하는지, 단일 입력 SQL transform equality predicate가 storage `partition_filter`와 compute input file list로 전달되는지 검증한다. 현재는 S49 read/preview + manifest metadata + local/fake/S3/Iceberg multi-part commit + high-cardinality warning + Iceberg file-level pruning + transform predicate-to-storage proof partial slice다.
- Iceberg maintenance runtime 게이트(`quality:iceberg-maintenance`)는 S50 planning+execution slice를 검증한다. Operations maintenance plan은 compaction candidate, orphan snapshot, retained snapshot, protected snapshot을 preview하고, DB committed dataset version이 참조하는 Iceberg snapshot을 삭제 후보에서 제외하며, plan 요청 audit evidence를 남겨야 한다. Maintenance run은 current snapshot을 compact snapshot으로 rewrite하고, compaction 전후 row-hash 동일성을 검증하며, 삭제 가능한 orphan snapshot만 expire/cleanup하고, protected snapshot과 cleanup 결과를 audit evidence로 남겨야 한다. Transform/materialization/backup pin 전체 보존 정책, concurrent maintenance fencing, ambiguous retry idempotency는 future work다.
- Continuous CDC worker runtime 게이트(`quality:cdc-continuous-worker`, `quality:kafka-rebalance`)는 S51 bounded stream archive loop와 CDC object-indexer bounded/continuous loop의 lease/cursor slice를 검증한다. Stream archive worker는 기존 `archive_stream_events` transaction/checkpoint boundary를 여러 batch에 반복 적용하고, configured empty poll/stop callback/max batch에서 멈추며, `workflow_runs` owner/token/expiry lease를 남기고, lease를 잃은 old worker가 다음 batch를 archive하지 않는 proof를 남겨야 한다. 또한 stream pre-commit assignment guard가 revoke 감지 시 already-read batch를 dataset version/cursor로 commit하지 않는지 검증한다. CDC object-indexer worker는 committed CDC dataset version cursor를 `workflow_runs.output.cdcObjectIndexer.cursor`에 남기고, restart/takeover 때 이미 index한 version을 건너뛰며, row-count limit 초과 version을 일부 처리 성공으로 만들지 않고 fail-closed 해야 한다. Full broker rebalance callback integration, broker commit-unknown reconciliation, multi-Sync/multi-partition supervisor, orchestrated multi-replica deployment packaging은 future work다. 현재 one-Sync resident supervisor, browser Start/Stop/status, worker lease/heartbeat/takeover, Kraken WebSocket v2 → Kafka → Dataset telemetry는 active slice다.
- Temporal engine integration runtime 게이트(`quality:temporal-engine-integration`)는 S52 product workflow control-plane 및 worker-bound connector data-plane slice를 검증한다. `ConnectorSyncWorkflow`는 Operations/API/generated SDK에서 stable `Idempotency-Key` 기반 workflow id로 시작/조회되고, local/fake/Temporal profile이 같은 `ProductWorkflowRun` 계약과 audit-linked `workflowRunId`/`foundryRunId` 증거를 남겨야 한다. Worker-bound connector activity는 deterministic `syncRunId`와 `workflowDefinitionVersion`으로 Dataset ingest boundary의 fetch/stage/quality/commit/cursor/event evidence를 남기며, post-commit activity retry가 duplicate dataset version/outbox를 만들지 않아야 한다. Activity 완료 후 start 응답이 유실되어 `start_unknown`이 된 run은 같은 idempotency key 재시작에서 adapter lookup으로 terminal workflow result에 재부착되어 `workflow.reconciled` audit/run relation evidence를 남기고, 처음 ledger에 저장된 `workflowDefinitionVersion`을 보존해야 한다. Workflow cancel은 requested/running run을 `cancelled` ledger state로 닫고 cleanup payload, `workflow.cancelled` audit, `cancelled_by` run relation evidence를 남기며, mismatched 또는 terminal non-cancelled adapter result를 거부해야 한다. Production connector packaging, continue-as-new, full old-history workflow upgrade replay, managed worker operations는 future work다.
- External writeback runtime 게이트(`quality:external-writeback`, `quality:action-writeback-retryable`, `quality:action-writeback-approval-release`, `quality:saga-reconciliation`, `quality:writeback-reconciliation-worker`, `quality:agent-vendor-egress`)는 S53 unresolved writeback slices를 검증한다. Simulated before-commit writeback의 외부 미변경 실패는 `action_runs.status="retryable"`, `action_writebacks.status="retryable"`, `external_system_changed=false`, `last_observed_status="not_changed"`, `retry_after_seconds`, audit evidence로 남기며 bounded recovery는 자동 reconcile/reissue 없이 `retryable_external_not_changed` skip item을 남겨야 한다. 응답 유실/timeout 성격 상황은 `failed`로 덮지 말고 `action_runs.status="outcome_unknown"`, `action_writebacks.status="outcome_unknown"`, `external_operation_id`, `idempotency_key`, `request_hash`, `last_observed_status`, `reconciliation_deadline`, audit evidence를 남겨야 한다. Simulated external success 뒤 local mutation failure는 `compensation_required`, `remote_resource_id`, `compensation_action_type` evidence를 남겨야 하며, 같은 idempotency key replay는 새 writeback을 만들면 안 된다. 같은 gate는 operator가 확인한 remote success evidence를 `POST /api/operations/reconciliation/{writeback_id}/resolve`/generated SDK로 제출하면 원래 local object mutation을 따라잡고 action/writeback을 `reconciled`로 닫으며, bounded `worker:writeback-reconciliation` tick은 stored `externalWritebackUri`가 있는 unresolved row를 remote lookup으로 recover하고 `action.writeback.recovery_tick` audit/JSON evidence를 남겨야 한다. 민감 action parameter가 있는 unresolved writeback은 bounded recovery가 remote lookup/local mutation을 실행하지 않고 `operator_approval_required` skip item과 `approvalRequired`/`approvalReason` evidence를 남기며, `POST /api/operations/reconciliation/{writeback_id}/approve-recovery`/generated SDK `approveRecovery`가 명시적 approval id/reason을 받으면 `action.writeback.recovery_approved` audit 후 같은 reconciliation path로 복구한다. Retryable row는 approval-recoverable 대상이 아니다. AI Agent가 `http.request` 같은 direct vendor/API/generic executor tool을 published manifest와 allowlist에 넣어도 ToolBroker/Agent Runtime/Visual Builder가 fail-closed 해야 한다. Concurrent resolve는 한 winner만 남기는지, 민감 writeback parameter가 Operations/audit evidence에 raw로 노출되지 않는지도 검증한다. ERP-specific connector packaging, connector-backed vendor tool release policy, automatic retry/reissue worker, always-on managed compensation daemon, reverse/compensation executor, persistent reconciliation queue UI, visual approval UI/full review workflow는 future work다.
- Data quality contract runtime 게이트(`quality:data-contracts`)는 S54 slices를 검증한다. Dataset quality check result는 어떤 staged candidate fingerprint와 `dataset_schemas` row/version을 기준으로 검증됐는지 `checked_manifest_hash`, `validated_against_schema_version_id`, `validated_against_schema_version`으로 저장해야 하며, 활성 default DataContract version이 있으면 `data_contract_version_id`도 저장해야 한다. dataset commit path와 dataset quality repository contract는 이 reference를 잃으면 안 된다. 이후 같은 dataset에 새 schema version이 생겨도 historical check result는 당시 schema row/version에 pinned되어야 한다. 성공한 check result는 `PASS`로 남기고, warning severity 실패는 commit을 막지 않되 `WARN`으로 보여야 하며, 품질검사 후 staged candidate가 바뀌거나 hard failure가 나면 final storage commit 전에 `BLOCK_COMMIT`으로 reject해야 한다. Row-level `not_null`/`unique` quarantine check는 실패 record를 tenant-scoped Record DLQ에 `DATA_QUALITY_CONTRACT`로 격리하고, 정상 record만 남긴 staged candidate를 재검증한 뒤 commit해야 하며, 일반 Record DLQ replay로 data-quality quarantine row를 재주입하면 안 된다. Persisted quality contract check definitions는 `dataset_checks`를 source of truth로 사용해 API/SDK에서 create/list/update되고, enabled/config 변경은 활성 contract snapshot이 없을 때 이후 dataset commit validation에 적용되어야 한다. Default DataContract version은 `dataset_quality_contract_versions`에 check snapshot/schema reference/owner evidence를 저장하고 API/SDK에서 create/list/get/activate되며, 활성화되면 이후 commit validation은 live check row가 아니라 snapshot을 기준으로 실행되어야 한다. `accepted_values` check는 활성 contract snapshot에 pinned된 허용 목록 밖 category/status 값을 `BLOCK_COMMIT`로 막아야 한다. Dataset quality result history와 summary는 dataset 단위 API/SDK에서 persisted commit-time check result, manifest hash, schema-version reference, data contract version id, run id, transaction id, status/check-type count evidence를 보여야 한다. Operations run detail은 같은 dataset transaction의 `quality` summary/schema reference/checked manifest hash/data contract version id/check result와 data-quality quarantine failed-row sample을 보여야 한다. Custom contract-key activation, remaining DataContract policy surface, owner notification, dedicated failed-row sample UI, trend UI, production DB schema race proof는 future work다.
- OpenLineage P8 동적 lineage 게이트(`scripts/quality/check_openlineage_dynamic_lineage.py`)는 supply-chain demo smoke 후 `transform_runs`와 `lineage_edges`가 dataset version 단위로 일치하는지 검증하고, 누락/중복/비-version/실패 run lineage를 차단하며 OpenLineage-compatible RunEvent artifact를 남긴다.
- MVP performance smoke 게이트(`scripts/quality/check_mvp_performance_smoke.py`)는 CSV ingest, object index, object query, no-writeback action apply 시간을 JSON 리포트로 남긴다. `ci` profile은 빠른 release gate 증거이고, 100k/1M profile은 릴리스 직전 장시간 측정용이다.
- Trace continuity G16 게이트(`scripts/quality/check_trace_continuity.py`)는 supply-chain demo를 in-memory OpenTelemetry provider 아래에서 실행해 synthetic request span, required service spans, SQLAlchemy DB spans가 같은 trace에 있고 service span이 같은 `request_id`를 들고 있는지 검증한다.
- Adapter error trace-key 게이트(`scripts/quality/check_adapter_error_trace_keys.py`)는 adapter 실패가 FAILED run의 error payload에 `tenant_id`, `actor_user_id`, `request_id`, `run_id`, `correlation_id`, `adapter`를 남기는지 동적으로 검증한다.
- Flaky detector G19 게이트(`scripts/quality/check_flaky_detector.py`)는 release gate에서 pytest-randomly + pytest-xdist 테스트 명령을 3회 반복해 한 번이라도 실패하거나 결과 요약이 달라지면 차단한다.
- Coverage exclusion G6 게이트(`scripts/quality/check_pragma_no_cover_budget.py`)는 `# pragma: no cover`를 baseline 0으로 차단한다. 예외가 필요하면 이유 주석과 함께 baseline/문서 갱신이 필요하다.
- Layer coverage G8 게이트(`scripts/quality/check_tier_coverage_by_layer.py`)는 coverage JSON을 domain/application/infrastructure/API/CLI/worker 계층별로 나누어 각 계층 95% 이상을 강제한다.
- 테스트 순서 의존성과 race condition은 pytest-randomly + pytest-xdist 두 plugin이 동적으로 강제한다. unit/contract test는 cwd에 의존하지 않는다. assets path는 conftest의 `DEMO_ROOT` 헬퍼를 쓰거나 `Path(__file__).resolve().parents[N]` 휴리스틱과 `mutants/` 부모 fallback을 같이 박는다.
- 안전 표현식, parser, 비즈니스 규칙 같은 작은 함수에는 hypothesis property test를 추가한다 (`tests/unit/test_*_properties.py`). 예시 기반 테스트가 못 잡는 엣지 케이스를 강제로 검증한다.

## 금지

- 엔지니어링 문서를 읽지 않고 구조 변경을 시작하지 않는다.
- god-class, god-service, fat-router를 만들지 않는다.
- `core._...` private facade helper 테스트를 만들지 않는다. facade는 public forwarder만 제공하고, 내부 실패 경계는 public API, service 소유 module, port/adapter contract test 중 가장 좁고 정직한 경계에서 검증한다.
- 문서에 아직 구현되지 않은 PostgreSQL JSONB, real CEL, real ERP writeback, full Temporal product-workflow data-plane/managed worker operations, Alembic 운영 upgrade/rollback을 구현 완료처럼 쓰지 않는다.
- 테스트/CI 실패를 skip, xfail, flaky 처리로 우회하지 않는다.
